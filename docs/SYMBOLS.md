# Symbol translation

## Result

The original ARM64 database was processed with `tools/ida_translate_symbols.py`.
The script reads the names that IDA exposes from the ELF, demangles the C++
names where possible, classifies the result, and applies readable aliases back
into IDA. The run was configured with renaming enabled and finished with:

```json
{
  "apply_renames": true,
  "data": 505,
  "functions": 4714,
  "jump_thunks": 199,
  "plt_thunks": 3183,
  "rename_failures": [],
  "renamed": 8601,
  "translated_symbols": 8601
}
```

The full exports are in `symbols/`. The CSV is convenient for grep, a
spreadsheet, or a quick address lookup. The JSON preserves the same records
with explicit fields.

The 8,601 figure is an applied alias inventory, not a claim that the APK
contains an unstripped debug symbol table. The original shared object is
reported as stripped and has no `.symtab` or DWARF sections. Its defined
dynamic symbol table contains 6,506 rows. The larger alias inventory includes
separate IDA-visible PLT, jump-thunk, and data aliases, which is why it should
not be compared directly with the dynamic row count. The complete correction
and command-level counts are in
`artifacts/elf_symbol_table_audit_20260826.json`.

IDA's function survey also reports compiler-generated functions and other
analysis-created entries, so its total function count is not expected to equal
8,601. The original pre-persistence inventory reports 11,272 total functions,
9,627 with names, and 1,645 default `sub_` names. Those figures describe the
original IDA snapshot; the 8,601 count describes the reproducible alias import
and rename pass. The artifact contains 467 cumulative evidence-backed labels
in `artifacts/ida_semantic_labels.json`.
The IDB also retains the earlier inferred
`TClient_setSSLParameters_scriptCallback` label outside that artifact. None of
these semantic labels is part of the 8,601 alias inventory rows.

One older native callback review label has since been corrected. The source
function at `0xe06a8` was initially described as
`TServerFlying_clearStaticStrings`, but its cleared globals are used by the
TapJoy and video paths, not by `TServerFlying::animate`. The descriptive role
`Android_TapJoy_video_clearStaticStrings` is recorded as a source-only
correction, and no 2.2 target is assigned to it. See
`artifacts/spectron_static_callback_role_correction_20260827.json`.

## Spectron dynamic symbols

The supplied 2.2 library needs a more precise description than simply
"stripped." Its static symbol table and DWARF sections are absent, so the
original application source names are not available through `.symtab` or
debug metadata. Its dynamic table is still present, however. The table has
6,773 entries, 6,770 named entries, 6,595 section-defined entries, and 5,782
section-defined functions. The library also keeps `.dynstr`, which is why the
dynamic names are recoverable even though the static names are not.

Most application C++ names are obfuscated, but this is still valuable for
the connection work. The target exports 28 named JNI entry points and 256
section-defined functions in the retained CyaInt or CyaSSL family. The most
useful target anchors include:

| Target role | Dynamic name or target class | Address | Size |
| --- | --- | ---: | ---: |
| App connection helper | `_ZN10XJLBgarMnA7connectERK10C8THgaTQxFi` | `0x20ad98` | 596 |
| TLS connection | `_ZN6CyaInt14CyaSSL_connectEPNS_6CYASSLE` | `0x2d2bcc` | 500 |
| Certificate date check | `_ZN6CyaInt12ValidateDateEPKhhi` | `0x2c2940` | 788 |
| Host-name check | `_ZN6CyaInt24CyaSSL_check_domain_nameEPNS_6CYASSLEPKc` | `0x2d3358` | 132 |
| Verify-buffer loader | `_ZN6CyaInt29CyaSSL_CTX_load_verify_bufferEPNS_10CYASSL_CTXEPKhli` | `0x2d35d8` | 228 |
| Context verify mode | `_ZN6CyaInt33CyaSSL_CTX_set_verifyEPNS_10CYASSL_CTXEiPFiiPNS_21CYASSL_X509_STORE_CTXEE` | `0x2d29ac` | 60 |

These are native exports from the target ELF, not semantic labels copied from
the 1.8 database. The translated IDA copy uses `v18_` aliases for reviewed
cross-build matches, while the new audit preserves the raw target name beside
its value, size, type, binding, and section index. This distinction matters
when a target function has the same job but a different obfuscated C++ name.

The full inventory is in
`artifacts/spectron_symbol_table_audit_20260827.json`. It is generated by
`tools/generate_spectron_symbol_table_audit.py` and does not load either
library or contact a service.

### Completing retained `FUNC` boundaries

The dynamic table also helps with IDA database hygiene. The v319 database had
12 section-defined positive-size `FUNC` rows whose values were not IDA
function starts. A read-only audit showed valid AArch64 prologues, no
containing function, and no end overlap for all twelve. The v320 copy
materializes the exact ELF intervals and preserves the retained target names.

| Address | Size | Retained name |
| ---: | ---: | --- |
| `0x1b0140` | `0x4a0` | `_ZN10_YTgFa6HPk10bdqDgaFFMBERK10eY2wgaf6pw` |
| `0x1b281c` | `0x314` | `_ZN10EqV_Ka3Vx910KHqDgay4MBERK10i7FHgaP2lF` |
| `0x1b2b34` | `0x450` | `_ZN10EqV_Ka3Vx910ZCBDgaugWBERK10eY2wgaf6pw` |
| `0x1c6c94` | `0x34c` | `_ZN10_k_Bgam3zA10sqrSLaYGpTERK10i7FHgaP2lF` |
| `0x1c6fe4` | `0x900` | `_ZN10_k_Bgam3zA10KHqDgay4MBERK10i7FHgaP2lF` |
| `0x1caeb4` | `0x190` | `_ZN10Q8n_Fa6V5W10ZCBDgaugWBERK10eY2wgaf6pw` |
| `0x1cd73c` | `0x3c4` | `_ZN10_thLgaWjoI10KHqDgay4MBERK10i7FHgaP2lF` |
| `0x1cdb04` | `0x59c` | `_ZN10_thLgaWjoI10ZCBDgaugWBERK10eY2wgaf6pw` |
| `0x1dac5c` | `0x1b0` | `_ZN10s_YwgafWlw10PbVb4aCJD8ERK10eY2wgaf6pwS2_S2_` |
| `0x1df0bc` | `0x34c` | `_ZN10awDo2aJRkD10KHqDgay4MBERK10i7FHgaP2lF` |
| `0x1dfffc` | `0x41c` | `_ZN10EYKlVaL7UR10ZCBDgaugWBERK10eY2wgaf6pw` |
| `0x2bcf44` | `0x48` | `_Z17yajl_buf_truncateP10yajl_buf_tm` |

After reopening the v320 database, all 5,782 section-defined dynamic `FUNC`
rows have exact IDA starts. The 988 named dynamic rows without a function at
their value are left alone because the complete table includes objects,
undefined imports, and other non-function entries. The machine-readable
boundary and join records are
`artifacts/spectron_dynamic_symbol_boundaries_20260828.json` and
`artifacts/spectron_symbol_translation_inventory_20260828.json`.

The complete address-and-item audit adds the missing distinction between a
function mismatch and a data or import row. All 6,600 defined named symbols
resolve to an IDA item: 5,782 functions, 482 data items, and 336 other
non-code items. It records 1,901 exact retained dynamic names, 4,541 reviewed
`v18_` aliases, 151 other retained target aliases, seven linker-boundary alias
rows, and 170 undefined imports with no target address. Exact PLT veneer names
represent 169 imports, while the `__sF` object has no in-library veneer. See
`artifacts/spectron_dynamic_symbol_coverage_audit_20260828.json`, generated by
`tools/ida_audit_spectron_dynamic_symbol_coverage.py`.

### v321 source-side GUI boundaries

The v321 pass repairs the corresponding source-side gap before applying
semantic names to the target. Eleven positive-size source `FUNC` rows were
materialized in a disposable 1.8 database, then matched to the eleven
positive-size GUI rows in Spectron:

| 1.8 source | Spectron target | Applied alias | Confidence |
| ---: | ---: | --- | --- |
| `0x1abf80` | `0x1b0140` | `v18_GuiBitmapButtonCtrl_onRender_TPoint_const` | high |
| `0x1ae65c` | `0x1b281c` | `v18_GuiButtonCtrl_drawWithStyle_TRectangle_const` | medium |
| `0x1ae97c` | `0x1b2b34` | `v18_GuiButtonCtrl_drawWithProfile_TPoint_const` | high |
| `0x1c21b8` | `0x1c6c94` | `v18_GuiScrollCtrl_drawBackground_TRectangle_const` | high |
| `0x1c2508` | `0x1c6fe4` | `v18_GuiScrollCtrl_drawWithStyle_TRectangle_const` | high |
| `0x1c63a8` | `0x1caeb4` | `v18_GuiTextCtrl_drawWithProfile_TPoint_const` | high |
| `0x1c8bb8` | `0x1cd73c` | `v18_GuiTextEditCtrl_drawWithStyle_TRectangle_const` | high |
| `0x1c8f80` | `0x1cdb04` | `v18_GuiTextEditCtrl_drawWithProfile_TPoint_const` | high |
| `0x1d5fcc` | `0x1dac5c` | `v18_GuiArrayCtrl_onRenderColumnHeaders_TPoint_const_TPoint_const_TPoint_const` | high |
| `0x1da320` | `0x1df0bc` | `v18_GuiPopUpMenuCtrl_drawWithStyle_TRectangle_const` | high |
| `0x1dc260` | `0x1dfffc` | `v18_GuiProgressCtrl_drawWithProfile_TPoint_const` | high |

Ten pairs have normalized-shape agreement. The `GuiButtonCtrl::drawWithStyle`
pair is the one manual class-slot match. Both classes have the same local
method neighborhood, the same `Buttons` and `Taskbar.Button` style strings,
32 basic blocks, 39 branches, 16 calls, and two returns. The target body is
eight bytes and two instructions shorter because rebuilt wrapper code changes
the layout. The anchor record keeps that difference explicit.

The final v321 target audit reports 11,707 functions and zero default names in
the checked IDA families. It counts 6,228 translated `v18_` aliases, 417
target-only descriptive labels, 1,002 retained target names, seven JNI
exports, and 4,053 other IDA or PLT names. The complete dynamic-symbol audit
reports 6,770 named rows, including 5,782 exact functions, 482 data items, 336
other non-code items, and 170 undefined imports. Exact PLT veneers account for
169 imports; `__sF` is the one undefined object without an in-library veneer.

The v321 evidence is in
`artifacts/spectron_gui_missing_function_manual_translation_anchors_20260828.json`,
`artifacts/spectron_gui_missing_function_application_20260828.json`,
`artifacts/spectron_gui_missing_function_verification_20260828.json`,
`artifacts/spectron_name_coverage_audit_v321_20260828.json`,
`artifacts/spectron_dynamic_symbol_boundaries_v321_20260828.json`, and
`artifacts/spectron_dynamic_symbol_coverage_audit_v321_20260828.json`.

### v322 TGraalVar runtime aliases

The v322 pass translates twelve methods in the obfuscated `G0gxgajWBw`
implementation of `TGraalVar`. These were not accepted from name similarity
alone. Each row was checked against source and target Hex-Rays pseudocode,
the surrounding class-local method order, and the values passed through the
key virtual slots.

| 1.8 source | Spectron target | Applied alias | Confidence |
| ---: | ---: | --- | --- |
| `0x20d304` | `0x2136c4` | `v18_TGraalVar_receiveEvent_script_event` | high |
| `0x20e070` | `0x214520` | `v18_TGraalVar_getVarNames_bool_bool_bool` | high |
| `0x20e5c4` | `0x214a78` | `v18_parseDynamicFunctionParameters_char_const_std_va_list` | high |
| `0x20ec60` | `0x215148` | `v18_TGraalVar_executeStringFunctionF_TString_const_char_const` | high |
| `0x20f014` | `0x2154e0` | `v18_TGraalVar_saveString_TString_const_uint` | high |
| `0x20f17c` | `0x215660` | `v18_TGraalVar_saveLines_TString_const_uint` | high |
| `0x20f2ac` | `0x2157a8` | `v18_TGraalVar_loadString_TString_const` | high |
| `0x20f3bc` | `0x2158e4` | `v18_TGraalVar_setVarValueAsFloat_TString_const_double` | high |
| `0x20f474` | `0x2159f4` | `v18_TGraalVar_getVarValue_TString_const` | high |
| `0x20fc18` | `0x216174` | `v18_TGraalVar_setArrayCellObject_int_TGraalVar` | high |
| `0x20fe5c` | `0x216454` | `v18_TGraalVar_getVarValueAsFloat_TString_const` | high |
| `0x20ff2c` | `0x216558` | `v18_TGraalVar_updateArrayString_void` | high |

The event forwarder at `0x2136c4` keeps the source's one-block, 24-instruction
body. The other methods are still semantic matches even when their complete
feature records differ. The target uses `C8THgaTQxF` in place of the source
`TString`, and similarly replaces the source list and hash wrappers. The
dynamic-parameter parser retains all format cases, the save/load methods keep
the same path and stream operations, and the variable accessors preserve the
same primary lookup and persistent-hash fallback. The array setter at
`0x216174` is an important disambiguation because automatic matching had
assigned the nearby target string setter to the source string setter.

The v322 name audit has zero default names in the checked families and reports
6,240 translated `v18_` aliases. The complete dynamic-symbol audit reports
4,564 source-backed aliases, 1,878 exact retained names, 151 other retained
target names, seven linker-boundary aliases, and the same 169 PLT veneers and
one `__sF` import exception as v321. The target still has 5,782 exact dynamic
function starts, 482 data items, 336 other non-code items, and 170 undefined
imports. The final v322 database hash is
`af0f2361668f7cd375b33242a0b21591a53446c332c0e77c8a4e51e3c6bdf1ad`.

The anchor record is
`artifacts/spectron_tgraalvar_runtime_gap_manual_translation_anchors_20260829.json`.
It stores the source and target feature metrics, direct-call names, and the
SHA-256 fingerprint of each compact decompilation. The application and
reopen reports are
`artifacts/spectron_tgraalvar_runtime_gap_manual_translation_application_20260829.json`
and
`artifacts/spectron_tgraalvar_runtime_gap_manual_translation_verification_20260829.json`.
The checkpoint is
`artifacts/spectron_translation_checkpoint_20260829_v322.json`.

## Current Spectron cross-build labels

The latest disposable IDA copies extend the labels beyond the retained ELF
exports. The corrected v263 copy added three reviewed correspondences from
the 1.8 database. The v270 correction now separates the two GuiCanvas dialog
callbacks: `pushdialog` is at target `0x1b5cf8`, while `popdialog` is at target
`0x1b58c4`. The `TGraalVar` script trigger is at `0x216a64`, and the
Facebook graph upload callback is at `0x253544`. Their source counterparts
are `0x1b1a24`, `0x1b15f0`, `0x210374`, and `0x246104`. The dialog correction
is recorded in the v270 script-table artifact below. All of these are
high-confidence semantic matches, but rebuilt target helpers can change the
normalized feature record.

The v264 copy also labels 22 target-only Android bridge functions. These do
not have a source counterpart in the current 1.8 evidence set. Their names
are based on the target's decoded script table, exact Java method strings,
and reviewed pseudocode:

| Target address | Applied label | Evidence role |
| ---: | --- | --- |
| `0x24b4ec` | `spectron_deeplink_getdeeplinkdata` | Java `GetIntentData` byte payload |
| `0x24b61c` | `spectron_notifications_getpushnotificationdata` | Java `GetPushNotificationData` byte payload |
| `0x24b74c` | `spectron_getandroidversionname` | Android version-name wrapper |
| `0x24b958` | `spectron_quattro_android_getinstallerpackagename` | installer-package lookup |
| `0x24bfcc` | `spectron_googleplayservicesavailable` | Google Play availability |
| `0x24cb68` | `spectron_getandroidversioncode` | Android version-code wrapper |
| `0x24cd60` | `spectron_deeplink_cleardeeplinkdata` | clear intent data |
| `0x24cdd4` | `spectron_notifications_clearpushnotificationdata` | clear push data |
| `0x24ce48` | `spectron_registerforpushnotifications` | notification permission request |
| `0x24cebc` | `spectron_quattro_android_googleinappreview` | Google Play review request |
| `0x24cf30` | `spectron_clearallnotifications` | clear notification state |
| `0x24d100` | `spectron_checkgoogleplaylicensing` | Google Play licensing check |
| `0x24d33c` | `spectron_notifications_unsubscribetotopic` | Firebase topic unsubscribe |
| `0x24d458` | `spectron_notifications_subscribetotopic` | Firebase topic subscribe |
| `0x24d574` | `spectron_firebase_addeventdata` | Firebase event metadata |
| `0x24d73c` | `spectron_firebase_logevent` | Firebase event logging |
| `0x24d858` | `spectron_addnotification` | notification creation |
| `0x24efb4` | `spectron_setsigningcertificate` | Java signing-certificate setter |
| `0x24f0d0` | `spectron_setgoogleplaykey` | Java Google Play key setter |
| `0x24fdfc` | `spectron_androidgetjavastaticint` | generic Java static integer helper |
| `0x24fee4` | `spectron_androidgetjavastaticstring` | generic Java static string helper |
| `0x2531ec` | `spectron_androidsystempropertyget` | Android system-property bridge |

Two application-facing helpers needed a separate review. The table entry and
body at `0x24b958` both describe `getinstallerpackagename`: the target calls
`getPackageName()` and then `PackageManager.getInstallerPackageName()`. The
table entry at `0x24a9ec` is `getsignature`, and its body calls
`getPackageInfo(packageName, 0x40)`, reads `signatures[0]`, and calls
`toCharsString()`. The earlier notes had these body descriptions reversed and
also incorrectly associated `0x24a9ec` with `ANDROID_ID`. The Android ID
correspondence is `0x2502f4`, now recorded in the v265 legacy anchor artifact.

The generated cross-build evidence is in
`artifacts/spectron_gui_android_manual_translation_anchors_20260828.json`.
The target-only evidence is in
`artifacts/spectron_android_bridge_target_only_labels_20260828.json`.
Both IDA applications were reopened and verified with zero failures. The
corrected Android security evidence and its v267 checkpoint are listed below.
The current saved databases and their checkpoint records are listed in the
README current-status section.

## Current Spectron script-table callback labels

The v270 pass translated the remaining default callback names whose encoded
script-table entries could be decoded exactly. The target uses a rebuilt
0x30-byte record layout, so the target name field is at offset `0x10` and the
callback field is at offset `0x28`. The source table uses the older callback
offset `0x18`.

| 1.8 callback | Source | Spectron target | Applied label | Source cell | Target cell | Match |
| --- | ---: | ---: | --- | ---: | ---: | --- |
| `GuiCanvas_script_pushdialog` | `0x1b1a24` | `0x1b5cf8` | `v18_GuiCanvas_script_pushdialog` | `0x380598` | `0x3935f8` | layout change and correction |
| `GuiCanvas_script_popdialog` | `0x1b15f0` | `0x1b58c4` | `v18_GuiCanvas_script_popdialog` | `0x3805c8` | `0x393628` | normalized shape |
| `GuiCanvas_script_iscursoron` | `0x1af0cc` | `0x1b3284` | `v18_GuiCanvas_script_iscursoron` | `0x3805f8` | `0x393658` | normalized shape |
| `GuiControl_script_makeFirstResponder` | `0x1b8a9c` | `0x1bd324` | `v18_GuiControl_script_makeFirstResponder` | `0x381378` | `0x3943d8` | exact metrics |
| `GuiControl_script_repaint` | `0x1b7470` | `0x1bbc30` | `v18_GuiControl_script_repaint` | `0x381498` | `0x3944f8` | exact metrics |
| `GuiScrollCtrl_script_scrolltobottom` | `0x1c198c` | `0x1c6468` | `v18_GuiScrollCtrl_script_scrolltobottom` | `0x382018` | `0x395078` | exact metrics |
| `GuiScrollCtrl_script_scrolltotop` | `0x1c1980` | `0x1c645c` | `v18_GuiScrollCtrl_script_scrolltotop` | `0x382048` | `0x3950a8` | exact metrics |
| `TGraalVar_script_ignoreevent` | `0x20d59c` | `0x2139c8` | `v18_TGraalVar_script_ignoreevent` | `0x3877a8` | `0x39a8f8` | exact metrics |
| `TGraalVar_script_ignoreevents` | `0x20d58c` | `0x2139b8` | `v18_TGraalVar_script_ignoreevents` | `0x3877d8` | `0x39a928` | exact metrics |
| `TGraalVar_script_objecttype` | `0x20dd34` | `0x2141e4` | `v18_TGraalVar_script_objecttype` | `0x387a48` | `0x39ab98` | exact metrics |
| `TGraalVar_script_sortascending` | `0x210ce0` | `0x21743c` | `v18_TGraalVar_script_sortascending` | `0x387bf8` | `0x39ad48` | exact metrics |
| `TGraalVar_script_sortdescending` | `0x210cd8` | `0x217434` | `v18_TGraalVar_script_sortdescending` | `0x387c28` | `0x39ad78` | exact metrics |
| `TGraalVar_script_timershow` | `0x20eea4` | `0x215370` | `v18_TGraalVar_script_timershow` | `0x387cb8` | `0x39ae08` | exact metrics |
| `TTiles_script_gettileset` | `0x22f2dc` | `0x238edc` | `v18_TTiles_script_gettileset` | `0x387ec0` | `0x39b010` | layout change |
| `TTiles_script_gettilesettype` | `0x22f2ac` | `0x238ea0` | `v18_TTiles_script_gettilesettype` | `0x387ef0` | `0x39b040` | normalized shape |
| `TTiles_script_addtiledef` | `0x230034` | `0x239d60` | `v18_TTiles_script_addtiledef` | `0x387f50` | `0x39b0a0` | exact metrics |
| `TTiles_script_addtiledef2` | `0x230020` | `0x239d4c` | `v18_TTiles_script_addtiledef2` | `0x387f80` | `0x39b0d0` | exact metrics |

The machine-readable evidence is
`artifacts/spectron_script_table_surface_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_script_table_surface_anchors.py`. It
records 17 high-confidence anchors, including the corrected push and pop
assignment. The v270 database and all 17 names reopened with zero failures.

## Current Spectron runtime callback residual labels

The v271 pass translated nine residual helper callbacks whose roles are
defined by the code that installs them. All nine are complete feature-metric
matches between the 1.8 source and the stripped Spectron target.

| 1.8 role | Source | Spectron target | Applied label | Target installation | Match |
| --- | ---: | ---: | --- | --- | --- |
| `TStream_zipOpenFile` | `0xf04ec` | `0xf19c8` | `v18_TStream_zipOpenFile` | zopen slot `0x00` in `0xf2374` | exact metrics |
| `TStream_zipTellFile` | `0xf04f0` | `0xf19cc` | `v18_TStream_zipTellFile` | ztell slot `0x18` in `0xf2374` | exact metrics |
| `TStream_zipCloseFile` | `0xf0548` | `0xf1a24` | `v18_TStream_zipCloseFile` | zclose slot `0x28` in `0xf2374` | exact metrics |
| `TStream_zipErrorFile` | `0xf0550` | `0xf1a2c` | `v18_TStream_zipErrorFile` | zerror slot `0x30` in `0xf2374` | exact metrics |
| `zlib_zcalloc` | `0x289b80` | `0x296ff0` | `v18_zlib_zcalloc` | `deflateInit2` and `inflateInit2` | exact metrics |
| `zlib_zcfree` | `0x289b88` | `0x296ff8` | `v18_zlib_zcfree` | `deflateInit2` and `inflateInit2` | exact metrics |
| `yajl_internal_realloc` | `0x2af788` | `0x2bcd0c` | `v18_yajl_internal_realloc` | default allocator table `0x2bcd28` | exact metrics |
| `yajl_internal_free` | `0x2af794` | `0x2bcd18` | `v18_yajl_internal_free` | default allocator table `0x2bcd28` | exact metrics |
| `yajl_internal_malloc` | `0x2af79c` | `0x2bcd20` | `v18_yajl_internal_malloc` | default allocator table `0x2bcd28` | exact metrics |

The TStream rows are installed by
`v18_TStream_fillZipFunctions_zlib_filefunc_def_s` at `0xf2374`. The target
zlib allocator pair is installed by `v18_deflateInit2` and
`v18_inflateInit2`. The YAJL allocator trio is installed by
`v18_yajl_set_default_alloc_funcs_yajl_alloc_funcs` at `0x2bcd28`. These
installation sites make the role assignments stronger than a size-only or
address-only match.

The machine-readable evidence is
`artifacts/spectron_runtime_callback_residual_manual_translation_anchors_20260828.json`.
The nine aliases were applied to the v271 IDA copy and reopened with zero
failures.

## Current Spectron target-only TPlayer property labels

The same pass labels a target-added property that has no demonstrated 1.8
counterpart. The target property record at `0x38ee08` points to the readable
name at `0x2e27c0`, has flags `0x6200`, and stores the getter and setter at
`0x38ee18` and `0x38ee20`.

| Target address | Applied label | Property | Role | Source counterpart |
| ---: | --- | --- | --- | --- |
| `0x170334` | `spectron_TPlayer_get_useQuattroZoomFactorCulling` | `Quattro::Rendering::Quattro2D::useQuattroZoomFactorCulling` | getter | not demonstrated |
| `0x170344` | `spectron_TPlayer_set_useQuattroZoomFactorCulling` | `Quattro::Rendering::Quattro2D::useQuattroZoomFactorCulling` | setter | not demonstrated |

The getter returns the byte held by the target's obfuscated TPlayer global,
and the setter stores and returns the incoming byte. The source inventory and
source binary were checked for the exact target property literal and contain
no matching record. The target-only evidence is in
`artifacts/spectron_tplayer_quattro_zoom_property_target_only_labels_20260828.json`.
The v271 checkpoint records both labels and the final residual count of 670
default names.

## Current Spectron libjpeg I/O callback labels

The v273 pass translated the six standard libjpeg callbacks that remain
unnamed in the stripped target. Their roles are identified by the callback
slots populated by `v18_jpeg_stdio_dest` and `v18_jpeg_stdio_src`, then
checked against the corresponding 1.8 bodies and feature metrics.

| 1.8 role | Source | Spectron target | Applied label | Target installation | Match |
| --- | ---: | ---: | --- | --- | --- |
| `init_destination` | `0x28b9f4` | `0x298e64` | `v18_jpeg_init_destination` | `v18_jpeg_stdio_dest` at `0x298fdc` | exact metrics |
| `empty_output_buffer` | `0x28ba30` | `0x298ea0` | `v18_jpeg_empty_output_buffer` | `v18_jpeg_stdio_dest` at `0x298fe8` | normalized shape |
| `term_destination` | `0x28baa4` | `0x298f14` | `v18_jpeg_term_destination` | `v18_jpeg_stdio_dest` at `0x298ff4` | normalized shape |
| `init_source` | `0x28bbbc` | `0x29902c` | `v18_jpeg_init_source` | `v18_jpeg_stdio_src` at `0x299220` | exact metrics |
| `fill_input_buffer` | `0x28bbcc` | `0x29903c` | `v18_jpeg_fill_input_buffer` | `v18_jpeg_stdio_src` at `0x29922c` | normalized shape |
| `skip_input_data` | `0x28bc88` | `0x2990f8` | `v18_jpeg_skip_input_data` | `v18_jpeg_stdio_src` at `0x299238` | normalized shape |

The destination callbacks initialize a 4096-byte buffer, write full and
partial buffers through JFWRITE, flush through JFFLUSH, and report JFERROR
through the JPEG error manager. The source callbacks set `start_of_file`, read
through JFREAD, install the JPEG EOI marker `FF D9` on an empty read, and
advance the source cursor when skipping input. Four rows differ only in
register allocation detail; two have identical complete feature metrics.

The machine-readable evidence is
`artifacts/spectron_jpeg_io_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_jpeg_io_anchors.py`. All six aliases were
applied to the v273 IDA copy and reopened with zero failures.

## Current Spectron libjpeg input-controller labels

The v274 pass translated the four standard `jdinput.c` callbacks installed by
the target `jinit_input_controller` routine. The role assignment uses the
controller slot, the body and caller behavior, the corresponding 1.8 body,
and the normalized feature record. The standard callback responsibilities are
also visible in the [libjpeg-turbo `jdinput.c` source](https://github.com/libjpeg-turbo/libjpeg-turbo/blob/main/src/jdinput.c).

| 1.8 role | Source | Spectron target | Applied label | Controller slot | Match |
| --- | ---: | ---: | --- | --- | --- |
| `finish_input_pass` | `0x28be44` | `0x2992b4` | `v18_jpeg_finish_input_pass` | `finish_input_pass` | normalized shape |
| `reset_input_controller` | `0x28be58` | `0x2992c8` | `v18_jpeg_reset_input_controller` | `reset_input_controller` | normalized shape |
| `start_input_pass` | `0x28beb0` | `0x299320` | `v18_jpeg_start_input_pass` | `start_input_pass` | exact metrics |
| `consume_markers` | `0x28c378` | `0x2997e8` | `v18_jpeg_consume_markers` | `consume_input` | exact metrics |

The target initializer at `0x299fb8` installs these callbacks in the same
order as the 1.8 controller. `v18_jpeg_consume_markers` reads marker state and
performs the initial image and scan setup, while
`v18_jpeg_start_input_pass` builds MCU state, materializes missing quantization
tables, starts entropy and coefficient passes, and selects `consume_data`.
The reset helper restores the marker consumer, clears the controller flags,
resets the error and marker readers, and clears coefficient bits. The finish
helper switches control back to marker consumption.

The machine-readable evidence is
`artifacts/spectron_jpeg_input_controller_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_jdinput_controller_anchors.py`. All four
aliases were applied to the v273 IDA copy and reopened as the v274 database
with zero failures.

## Current Spectron libjpeg marker-reader labels

The v275 pass translated the nine residual `jdmarker.c` roles around the
target marker-reader initializer. The direct callback table resolves the
reader, reset, restart, skip, and APPn processing roles. Internal call sites
and body behavior resolve the SOF, DHT, APP0, and saved-marker helpers. The
upstream responsibilities are described in the [libjpeg-turbo `jdmarker.c` source](https://github.com/libjpeg-turbo/libjpeg-turbo/blob/main/src/jdmarker.c).

| 1.8 role | Source | Spectron target | Applied label | Target evidence | Match |
| --- | ---: | ---: | --- | --- | --- |
| `get_sof` | `0x28cbb8` | `0x29a028` | `v18_jpeg_get_sof` | SOF dispatches in `read_markers` | exact metrics |
| `examine_app0` | `0x28cffc` | `0x29a46c` | `v18_jpeg_examine_app0` | APP0 paths in marker saving and APPn processing | exact metrics |
| `skip_variable` | `0x28d2ec` | `0x29a75c` | `v18_jpeg_skip_variable` | default COM and APPn processor slot | exact metrics |
| `reset_marker_reader` | `0x28d400` | `0x29a870` | `v18_jpeg_reset_marker_reader` | marker-reader reset slot | exact metrics |
| `get_dht` | `0x28d424` | `0x29a894` | `v18_jpeg_get_dht` | DHT dispatch in `read_markers` | exact metrics |
| `save_marker` | `0x28d804` | `0x29ac74` | `v18_jpeg_save_marker` | `jpeg_save_markers` selection | exact metrics |
| `get_interesting_appn` | `0x28db3c` | `0x29afac` | `v18_jpeg_get_interesting_appn` | default APP0 and APP14 slots | exact metrics |
| `read_markers` | `0x28dd9c` | `0x29b20c` | `v18_jpeg_read_markers` | marker-reader read slot | normalized shape |
| `read_restart_marker` | `0x28eb20` | `0x29bf90` | `v18_jpeg_read_restart_marker` | marker-reader restart slot | exact metrics |

The target `v18_jinit_marker_reader_jpeg_decompress_struct` routine at
`0x29c498` installs `reset_marker_reader` at the first callback slot,
`read_markers` at the read slot, and `read_restart_marker` at the restart
slot. It initially uses `skip_variable` for generic COM and APPn markers and
`get_interesting_appn` for APP0 and APP14. `jpeg_save_markers` can replace
those handlers with `save_marker` when the caller requests marker retention.
The large `read_markers` body contains the compiler-inlined SOI, SOS, DQT,
DRI, and next-marker logic, so those source helper names do not correspond to
separate target function starts in this build.

The machine-readable evidence is
`artifacts/spectron_jpeg_marker_reader_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_jdmarker_anchors.py`. All nine aliases
were applied to the v274 IDA copy and reopened as the v275 database with zero
failures.

## Current Spectron libjpeg output-pipeline labels

The v276 pass continued into the adjacent `jdmaster.c` and `jdmerge.c` blocks.
The master-decompress initializer installs the output-pass callbacks, while
the merged-upsampler initializer chooses the one-row or two-row wrapper and
the matching color-conversion method. The standard responsibilities are
described in the [libjpeg-turbo `jdmaster.c` source](https://github.com/libjpeg-turbo/libjpeg-turbo/blob/main/src/jdmaster.c)
and [libjpeg-turbo `jdmerge.c` source](https://github.com/libjpeg-turbo/libjpeg-turbo/blob/main/src/jdmerge.c).

| 1.8 role | Source | Spectron target | Applied label | Target evidence | Match |
| --- | ---: | ---: | --- | --- | --- |
| `prepare_for_output_pass` | `0x28f2b0` | `0x29c720` | `v18_jpeg_prepare_for_output_pass` | master callback field +0 | exact metrics |
| `finish_output_pass` | `0x28f478` | `0x29c8e8` | `v18_jpeg_finish_output_pass` | master callback field +8 | exact metrics |
| `start_pass_merged_upsample` | `0x28fee0` | `0x29d350` | `v18_jpeg_start_pass_merged_upsample` | merged-upsampler start callback | exact metrics |
| `merged_1v_upsample` | `0x28fef4` | `0x29d364` | `v18_jpeg_merged_1v_upsample` | one-row wrapper branch | exact metrics |
| `h2v1_merged_upsample` | `0x28ff44` | `0x29d3b4` | `v18_jpeg_h2v1_merged_upsample` | one-row conversion method | exact metrics |
| `h2v2_merged_upsample` | `0x290094` | `0x29d504` | `v18_jpeg_h2v2_merged_upsample` | two-row conversion method | exact metrics |
| `merged_2v_upsample` | `0x290294` | `0x29d704` | `v18_jpeg_merged_2v_upsample` | two-row wrapper branch | exact metrics |

The target master initializer at `0x29cd30` stores
`v18_jpeg_prepare_for_output_pass` and `v18_jpeg_finish_output_pass` in the
master state callback pair. The first routine selects the active quantizer,
starts the output modules, and prepares pass bookkeeping. The second finishes
the two-pass quantizer when present and advances the master pass counter.

The target merged-upsampler initializer at `0x29d808` always installs
`v18_jpeg_start_pass_merged_upsample`. When the maximum vertical sampling
factor is one, it selects `v18_jpeg_merged_1v_upsample` with
`v18_jpeg_h2v1_merged_upsample`. When that factor is two, it selects
`v18_jpeg_merged_2v_upsample` with `v18_jpeg_h2v2_merged_upsample`. The h2v1
body expands one chroma row across pairs of output pixels. The h2v2 body
does the same for two Y rows, and the two wrapper routines manage row-group
and spare-row state around those conversion methods.

The machine-readable evidence is
`artifacts/spectron_jpeg_master_merge_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_jdmaster_jdmerge_anchors.py`. All seven
aliases were applied to the v275 IDA copy and reopened as the v276 database
with zero failures. The v276 checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v276.json`; it records
11,696 functions and 643 remaining default `sub_` names.

## Current Spectron libjpeg progressive-Huffman labels

The v277 pass translated the five unnamed routines immediately before the
target's `v18_jinit_phuff_decoder_jpeg_decompress_struct` initializer. The
initializer at `0x29ea4c` installs the start-pass dispatcher. That dispatcher
selects the MCU decoder based on whether the scan is a DC or AC band and
whether it is a first or successive-approximation scan. The role names follow
the [libjpeg-turbo `jdphuff.c` implementation](https://github.com/libjpeg-turbo/libjpeg-turbo/blob/main/src/jdphuff.c).

| 1.8 role | Source | Spectron target | Applied label | Target dispatch | Match |
| --- | ---: | ---: | --- | --- | --- |
| `start_pass_phuff_decoder` | `0x290538` | `0x29d9a8` | `v18_jpeg_start_pass_phuff_decoder` | entropy start-pass slot | normalized shape |
| `decode_mcu_AC_refine` | `0x29095c` | `0x29ddcc` | `v18_jpeg_decode_mcu_AC_refine` | AC with `Ah != 0` | normalized shape |
| `decode_mcu_AC_first` | `0x290e3c` | `0x29e2ac` | `v18_jpeg_decode_mcu_AC_first` | AC with `Ah == 0` | normalized shape |
| `decode_mcu_DC_refine` | `0x291154` | `0x29e5c4` | `v18_jpeg_decode_mcu_DC_refine` | DC with `Ah != 0` | exact metrics |
| `decode_mcu_DC_first` | `0x2912f8` | `0x29e768` | `v18_jpeg_decode_mcu_DC_first` | DC with `Ah == 0` | normalized shape |

The start-pass body validates the progressive scan bounds and approximation
levels, updates `coef_bits`, selects the correct decoder, builds derived
Huffman tables, resets the bit reader and EOB state, and initializes the
restart counter. The AC refinement body handles new coefficients, correction
bits, ZRL, EOBRUN, and rollback when input suspends. The AC first body decodes
run and size nibbles and writes dezigzagged coefficients. The DC refinement
body reads one refinement bit per block. The DC first body decodes signed DC
differences, updates the component predictors, and scales the coefficient by
`Al`.

The source and target feature records match in every normalized field. The DC
refinement row is a complete metric match. The other four rows differ only in
register allocation detail, which is consistent with separate compiler builds.
The machine-readable evidence is
`artifacts/spectron_jpeg_progressive_huffman_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_jdphuff_anchors.py`. All five aliases
were applied to the v276 IDA copy and reopened as the v277 database with zero
failures. The v277 checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v277.json`; it records
11,696 functions and 638 remaining default `sub_` names.

## Current Spectron libjpeg postprocessing labels

The v278 pass translated the four unnamed routines dispatched by the target
`v18_jinit_d_post_controller_jpeg_decompress_struct_int` initializer at
`0x29ef00`. The start-pass routine chooses the processing callback from the
buffer mode. The roles follow the [libjpeg-turbo `jdpostct.c` implementation](https://github.com/libjpeg-turbo/libjpeg-turbo/blob/main/src/jdpostct.c).

| 1.8 role | Source | Spectron target | Applied label | Target dispatch | Match |
| --- | ---: | ---: | --- | --- | --- |
| `start_pass_dpost` | `0x2916f8` | `0x29eb68` | `v18_jpeg_start_pass_dpost` | post-controller start slot | normalized shape |
| `post_process_1pass` | `0x291810` | `0x29ec80` | `v18_jpeg_post_process_1pass` | one-pass quantization | exact metrics |
| `post_process_prepass` | `0x2918a0` | `0x29ed10` | `v18_jpeg_post_process_prepass` | two-pass first pass | exact metrics |
| `post_process_2pass` | `0x2919a0` | `0x29ee10` | `v18_jpeg_post_process_2pass` | two-pass output pass | exact metrics |

The start-pass body handles pass-through, one-pass quantization, and both
two-pass quantization modes, then resets the strip cursors. The one-pass body
feeds the upsampler's strip into the color quantizer and emits rows. The
prepass body stores upsampled rows in a virtual image and lets the quantizer
scan them without producing output. The second-pass body reads the virtual
strip, clamps the number of rows to the strip, output-area, and image-height
limits, then emits quantized rows.

All four source and target rows match in normalized ARM64 feature shape. Three
are complete metric matches, and `start_pass_dpost` differs only in register
allocation detail. The machine-readable evidence is
`artifacts/spectron_jpeg_postprocessing_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_jdpostct_anchors.py`. All four aliases
were applied to the v277 IDA copy and reopened as the v278 database with zero
failures. The v278 checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v278.json`; it records
11,696 functions and 634 remaining default `sub_` names.

## Current Spectron libjpeg upsampler labels

The v279 pass translated the nine unnamed callbacks in the adjacent
`jdsample.c` block. The target initializer
`v18_jinit_upsampler_jpeg_decompress_struct` at `0x29fc9c` installs the
public start and row callbacks, then selects a component method from the
sampling factors, the fancy-upsampling flag, and whether the component is
needed. The role names follow the [libjpeg-turbo `jdsample.c` implementation](https://github.com/libjpeg-turbo/libjpeg-turbo/blob/main/src/jdsample.c).

| 1.8 role | Source | Spectron target | Applied label | Target dispatch | Match |
| --- | ---: | ---: | --- | --- | --- |
| `start_pass_upsample` | `0x291b6c` | `0x29efdc` | `v18_jpeg_start_pass_upsample` | upsampler start slot | exact metrics |
| `sep_upsample` | `0x291b84` | `0x29eff4` | `v18_jpeg_sep_upsample` | public upsample slot | exact metrics |
| `fullsize_upsample` | `0x291ce8` | `0x29f158` | `v18_jpeg_fullsize_upsample` | sampling factors already match | exact metrics |
| `noop_upsample` | `0x291cf0` | `0x29f160` | `v18_jpeg_noop_upsample` | component not needed | exact metrics |
| `h2v1_upsample` | `0x291cf8` | `0x29f168` | `v18_jpeg_h2v1_upsample` | simple horizontal 2:1 | exact metrics |
| `h2v1_fancy_upsample` | `0x291f6c` | `0x29f3dc` | `v18_jpeg_h2v1_fancy_upsample` | fancy horizontal 2:1 | exact metrics |
| `h1v2_fancy_upsample` | `0x292220` | `0x29f690` | `v18_jpeg_h1v2_fancy_upsample` | fancy vertical 2:1 | exact metrics |
| `int_upsample` | `0x292360` | `0x29f7d0` | `v18_jpeg_int_upsample` | other integral factors | exact metrics |
| `h2v2_upsample` | `0x292568` | `0x29f9d8` | `v18_jpeg_h2v2_upsample` | simple vertical 2:1 | exact metrics |

The target start-pass routine records the output height and maximum vertical
sampling factor. The row callback drains converted rows, invokes each
component method for new input rows, and advances the decompressor row state.
The full-size method passes a row through, while the no-op method suppresses
an unused component. The simple h2v1 and h2v2 methods duplicate samples, the
fancy methods interpolate neighboring samples, and the generic method applies
the integral expansion factors selected by the initializer. The target body
at `0x29f690` also causes the initializer to request context rows, as the
vertical fancy path requires adjacent input rows.

All nine source and target rows match the complete recorded ARM64 feature
set, including register allocation detail. Both sides were default names
before this pass, so these `v18_jpeg_` labels are reviewed semantic roles
rather than recovered debug symbols. The machine-readable evidence is
`artifacts/spectron_jpeg_upsampler_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_jdsample_anchors.py`. All nine aliases
were applied to a copy of the v278 IDA database and reopened as the v279
database with zero failures. The v279 checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v279.json`; it records
11,696 functions and 625 remaining default `sub_` names.

## Current Spectron libjpeg error-handler labels

The v280 pass translated the five unnamed callbacks assigned by the target
`v18_jpeg_std_error` initializer at `0x2a00fc`. These routines are the
standard `jerror.c` message, reset, formatting, output, and fatal-exit
handlers. The role names follow the [libjpeg-turbo `jerror.c` implementation](https://github.com/libjpeg-turbo/libjpeg-turbo/blob/main/src/jerror.c).

| 1.8 role | Source | Spectron target | Applied label | Target callback field | Match |
| --- | ---: | ---: | --- | --- | --- |
| `emit_message` | `0x292aac` | `0x29ff1c` | `v18_jpeg_emit_message` | `jpeg_error_mgr.emit_message` | exact metrics |
| `reset_error_mgr` | `0x292b24` | `0x29ff94` | `v18_jpeg_reset_error_mgr` | `jpeg_error_mgr.reset_error_mgr` | exact metrics |
| `format_message` | `0x292b34` | `0x29ffa4` | `v18_jpeg_format_message` | `jpeg_error_mgr.format_message` | exact metrics |
| `output_message` | `0x292c1c` | `0x2a008c` | `v18_jpeg_output_message` | `jpeg_error_mgr.output_message` | normalized shape |
| `error_exit` | `0x292c64` | `0x2a00d4` | `v18_jpeg_error_exit` | `jpeg_error_mgr.error_exit` | exact metrics |

The target `jpeg_std_error` body preserves the source callback order. The
message emitter shows the first warning, or trace messages allowed by the
current trace level, and increments the warning counter. The reset handler
clears the warning count and message code. The formatter selects a built-in
or add-on message, handles the `%s` case, and formats the saved parameters.
The output handler formats into a local buffer and writes to stderr. The fatal
handler emits the message, destroys the JPEG object, and exits with status one.

All five source and target rows match in normalized ARM64 feature shape. Four
match the complete recorded feature set, while `output_message` differs only
in register allocation detail. Both sides were default names before this
pass, so these `v18_jpeg_` labels are reviewed semantic roles rather than
recovered debug symbols. The machine-readable evidence is
`artifacts/spectron_jpeg_error_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_jerror_anchors.py`. All five aliases
were applied to a copy of the v279 IDA database and reopened as the v280
database with zero failures. The v280 checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v280.json`; it records
11,696 functions and 620 remaining default `sub_` names.

## Current Spectron libjpeg memory-manager labels

The v281 pass translated the eleven unnamed methods assigned by the target
`v18_jinit_memory_mgr_jpeg_common_struct` initializer at `0x2a21b8`. The
initializer preserves the public method table from `jmemmgr.c`, including
pool allocation, two-dimensional arrays, virtual arrays, pool release, and
full manager teardown. The role names follow the [libjpeg-turbo `jmemmgr.c` implementation](https://github.com/libjpeg-turbo/libjpeg-turbo/blob/main/src/jmemmgr.c).

| 1.8 role | Source | Spectron target | Applied label | Method slot | Match |
| --- | ---: | ---: | --- | ---: | --- |
| `alloc_small` | `0x292cf8` | `0x2a0168` | `v18_jpeg_alloc_small` | 0 | call-layout difference |
| `alloc_large` | `0x292ef8` | `0x2a0368` | `v18_jpeg_alloc_large` | 9 | exact metrics |
| `alloc_sarray` | `0x293058` | `0x2a04c8` | `v18_jpeg_alloc_sarray` | 1 | exact metrics |
| `alloc_barray` | `0x293168` | `0x2a05d8` | `v18_jpeg_alloc_barray` | 7 | exact metrics |
| `realize_virt_arrays` | `0x293500` | `0x2a0970` | `v18_jpeg_realize_virt_arrays` | 8 | exact metrics |
| `request_virt_sarray` | `0x2938b0` | `0x2a0d20` | `v18_jpeg_request_virt_sarray` | 4 | register detail |
| `request_virt_barray` | `0x293acc` | `0x2a0f3c` | `v18_jpeg_request_virt_barray` | 5 | register detail |
| `access_virt_barray` | `0x293ce8` | `0x2a1158` | `v18_jpeg_access_virt_barray` | 10 | exact metrics |
| `access_virt_sarray` | `0x293e50` | `0x2a12c0` | `v18_jpeg_access_virt_sarray` | 2 | register detail |
| `free_pool` | `0x2941d0` | `0x2a1640` | `v18_jpeg_free_pool` | 3 | call-layout difference |
| `self_destruct` | `0x294534` | `0x2a19a4` | `v18_jpeg_self_destruct` | 6 | exact metrics |

The target method table assignments are direct evidence for the roles. The
small and large allocators manage aligned pool headers and account for total
space. The sample and coefficient array allocators create row-pointer tables
and chunked storage. The request methods create virtual-array control blocks,
while `realize_virt_arrays` divides available memory and creates their active
buffers. The access methods move strips through backing storage, zero newly
exposed rows, and mark writable buffers dirty. `free_pool` closes virtual
stores and releases both pool types. `self_destruct` frees all pools, clears
the manager pointer, and terminates system-dependent memory state.

All eleven source and target rows are high-confidence method-table matches.
Nine match normalized ARM64 features, six match every recorded feature field,
and three differ only in register allocation detail. `alloc_small` and
`free_pool` also differ in the recorded call and operand layout because the
1.8 body calls through PLT thunks while the target retains direct allocator
exports. Their decompiled pool logic, sizes, mnemonic counts, and method
assignments agree, so the two rows are recorded as explicit layout changes
instead of being presented as exact feature matches. Both sides were default
names before this pass, so these `v18_jpeg_` labels are reviewed semantic
roles rather than recovered debug symbols.

The machine-readable evidence is
`artifacts/spectron_jpeg_memory_manager_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_jmemmgr_anchors.py`. All eleven aliases
were applied to a copy of the v280 IDA database and reopened as the v281
database with zero failures. The v281 checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v281.json`; it records
11,696 functions and 609 remaining default `sub_` names.

## Current Spectron libjpeg one-pass quantizer labels

The v282 pass translated the eight unnamed callbacks in the adjacent
`jquant1.c` block. The target initializer
`v18_jinit_1pass_quantizer_jpeg_decompress_struct` at `0x2a2f88` installs the
start, finish, and new-color-map callbacks, then selects a row quantizer from
the dither mode and component count. The role names follow the
[libjpeg-turbo `jquant1.c` implementation](https://github.com/libjpeg-turbo/libjpeg-turbo/blob/main/src/jquant1.c).

| 1.8 role | Source | Spectron target | Applied label | Target dispatch | Match |
| --- | ---: | ---: | --- | --- | --- |
| `color_quantize` | `0x294f44` | `0x2a23b4` | `v18_jpeg_color_quantize` | general components, no dither | exact metrics |
| `color_quantize3` | `0x294fd0` | `0x2a2440` | `v18_jpeg_color_quantize3` | three components, no dither | exact metrics |
| `quantize3_ord_dither` | `0x295050` | `0x2a24c0` | `v18_jpeg_quantize3_ord_dither` | three components, ordered dither | exact metrics |
| `finish_pass_1_quant` | `0x295138` | `0x2a25a8` | `v18_jpeg_finish_pass_1_quant` | finish-pass callback, `nullsub_4` | exact metrics |
| `new_color_map_1_quant` | `0x29513c` | `0x2a25ac` | `v18_jpeg_new_color_map_1_quant` | new-color-map callback | exact metrics |
| `quantize_fs_dither` | `0x295164` | `0x2a25d4` | `v18_jpeg_quantize_fs_dither` | Floyd-Steinberg dither | exact metrics |
| `quantize_ord_dither` | `0x295344` | `0x2a27b4` | `v18_jpeg_quantize_ord_dither` | general components, ordered dither | exact metrics |
| `start_pass_1_quant` | `0x29545c` | `0x2a28cc` | `v18_jpeg_start_pass_1_quant` | start-pass callback and dispatcher | normalized shape |

The plain paths perform direct color-index lookups. The ordered paths apply
the sixteen-row dither tables, with a specialized RGB loop at `0x2a24c0`.
The Floyd-Steinberg path at `0x2a25d4` carries quantization error through its
workspace and reverses its scan direction on alternating rows. The
start-pass routine at `0x2a28cc` builds the palette dimensions and lookup
tables, initializes the selected dither workspace, and installs the active
row callback. The finish callback is the shared four-byte no-op, while the
new-color-map callback reports the unsupported operation through the JPEG
error manager.

All eight source and target rows are high-confidence matches and all eight
match in normalized ARM64 feature shape. Seven also match the complete
recorded feature set. The only remaining difference is register allocation
detail in `start_pass_1_quant`. The source and target `nullsub_4` entry is an
IDA auto-generated name, so the applied `v18_jpeg_` label is a reviewed
semantic role rather than recovered debug metadata.

The machine-readable evidence is
`artifacts/spectron_jpeg_one_pass_quantizer_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_jquant1_anchors.py`. All eight aliases
were applied to a copy of the v281 IDA database and reopened as the v282
database with zero failures. The v282 checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v282.json`; it records
11,696 functions and 602 remaining default `sub_` names.

## Current Spectron libjpeg two-pass quantizer labels

The v283 pass translated the nine retained function boundaries in the
adjacent `jquant2.c` block. The target initializer
`v18_jinit_2pass_quantizer_jpeg_decompress_struct` at `0x2a4f70` installs the
public start and new-color-map callbacks. Its start-pass routine selects the
histogram prescan, the non-dithered second pass, or the Floyd-Steinberg second
pass. The role names follow the [libjpeg-turbo `jquant2.c` implementation](https://github.com/libjpeg-turbo/libjpeg-turbo/blob/main/src/jquant2.c).

| 1.8 role | Source | Spectron target | Applied label | Target dispatch | Match |
| --- | ---: | ---: | --- | --- | --- |
| `prescan_quantize` | `0x296270` | `0x2a36e0` | `v18_jpeg_prescan_quantize` | histogram prescan at `0x2a37a8` | exact metrics |
| `finish_pass2` | `0x2962f4` | `0x2a3764` | `v18_jpeg_finish_pass2` | second-pass finish at `0x2a380c` | exact metrics |
| `new_color_map_2_quant` | `0x2962f8` | `0x2a3768` | `v18_jpeg_new_color_map_2_quant` | new-color-map slot at `0x2a4fb0` | exact metrics |
| `start_pass_2_quant` | `0x296308` | `0x2a3778` | `v18_jpeg_start_pass_2_quant` | public start slot at `0x2a4fa0` and `0x2a4fa8` | normalized shape |
| `update_box` | `0x296620` | `0x2a3a90` | `v18_jpeg_update_box` | palette-box split at `0x2a4cd4` and `0x2a4ce4` | exact metrics |
| `fill_inverse_cmap` | `0x296a64` | `0x2a3ed4` | `v18_jpeg_fill_inverse_cmap` | cache misses at `0x2a440c` and `0x2a4704` | exact metrics |
| `pass2_no_dither` | `0x296ee0` | `0x2a4350` | `v18_jpeg_pass2_no_dither` | no-dither mapping at `0x2a37fc` | exact metrics |
| `pass2_fs_dither` | `0x296fe4` | `0x2a4454` | `v18_jpeg_pass2_fs_dither` | Floyd-Steinberg mapping at `0x2a3874` | exact metrics |
| `finish_pass1` | `0x297388` | `0x2a47f8` | `v18_jpeg_finish_pass1` | prescan finish at `0x2a37b4` | exact metrics |

The prescan path builds the reduced-precision RGB histogram. The finish path
shrinks and splits the occupied color-space boxes, computes representative
palette colors, and marks the inverse map for the next pass. The second-pass
plain path uses the inverse-map cache directly. The Floyd-Steinberg path
alternates scan direction, applies the error limiter, and carries three
component errors through its workspace. The new-color-map callback marks the
cache for clearing, while the second-pass finish callback is the shared
four-byte no-op.

The source has separate local descriptions for nearby-color and best-color
search inside the inverse-map algorithm. In this target build those loops are
folded into the retained `fill_inverse_cmap` and palette-finish boundaries, so
the artifact labels the actual target function starts and does not invent
extra names for absent boundaries.

All nine source and target rows are high-confidence matches and all nine
match in normalized ARM64 feature shape. Eight also match the complete
recorded feature set. The only remaining difference is register allocation
detail in `start_pass_2_quant`. The source and target `nullsub_5` finish entry
is treated as an IDA auto-generated name, so the applied labels remain
reviewed semantic roles rather than recovered debug metadata.

The machine-readable evidence is
`artifacts/spectron_jpeg_two_pass_quantizer_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_jquant2_anchors.py`. All nine aliases
were applied to a copy of the v282 IDA database and reopened as the v283
database with zero failures. The v283 checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v283.json`; it records
11,696 functions and 594 remaining default `sub_` names.

## Current Spectron libjpeg coefficient-controller labels

The v284 pass translated the seven retained function boundaries around the
target `v18_jinit_d_coef_controller_jpeg_decompress_struct_int` initializer at
`0x2aad18`. The corresponding source initializer is at `0x29d8a8`. The role
names follow the [libjpeg-turbo `jdcoefct.c` implementation](https://github.com/libjpeg-turbo/libjpeg-turbo/blob/main/src/jdcoefct.c), which places this controller between entropy decoding and inverse DCT output.

| 1.8 role | Source | Spectron target | Applied label | Target use | Match |
| --- | ---: | ---: | --- | --- | --- |
| `dummy_consume_data` | `0x29c6bc` | `0x2a9b2c` | `v18_jpeg_dummy_consume_data` | single-MCU consume callback at `0x2aae7c` | exact metrics |
| `consume_data` | `0x29c6c4` | `0x2a9b34` | `v18_jpeg_consume_data` | full-buffer consume callback at `0x2aae04` and `0x2aae0c` | exact metrics |
| `start_output_pass` | `0x29c9c8` | `0x2a9e38` | `v18_jpeg_start_output_pass` | output-pass slot at `0x2aad60` | normalized shape |
| `decompress_smooth_data` | `0x29cb80` | `0x2a9ff0` | `v18_jpeg_decompress_smooth_data` | block-smoothing path at `0x2a9fa8` | exact metrics |
| `decompress_data` | `0x29d2f8` | `0x2aa768` | `v18_jpeg_decompress_data` | normal full-buffer output at `0x2a9e5c` and `0x2aae14` | exact metrics |
| `start_input_pass` | `0x29d510` | `0x2aa980` | `v18_jpeg_coef_start_input_pass` | input-pass slot at `0x2aad54` | exact metrics |
| `decompress_onepass` | `0x29d570` | `0x2aa9e0` | `v18_jpeg_decompress_onepass` | single-MCU output at `0x2aae88` and `0x2aae94` | exact metrics |

The full-buffer path stores one virtual coefficient array per component. Its
`consume_data` callback builds row pointers, calls the entropy decoder, and
advances the input state. Its normal `decompress_data` callback keeps input
ahead of output, reads those arrays, and calls each component's inverse-DCT
method. The single-MCU path uses the dummy consume callback and
`decompress_onepass`, which clears the MCU buffer, decodes one row, and emits
only useful blocks. The output-pass setup chooses the normal or smoothed
callback, while the smoothing callback retains the neighboring-DC estimation
and workspace path.

All seven source and target rows are high-confidence matches and all seven
match normalized ARM64 feature shape. Six also match the complete recorded
feature set. The only difference is register allocation detail in
`start_output_pass`. The `start_input_pass` routine in this coefficient
controller is file-local and shares its source name with the already labeled
`jdinput.c` routine at target `0x299320`, so the applied label uses the
`coef_` qualifier to keep both IDA names unique.

The machine-readable evidence is
`artifacts/spectron_jpeg_coefficient_controller_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_jdcoefct_anchors.py`. All seven aliases
were applied to a copy of the v283 IDA database and reopened as the v284
database with zero failures. The v284 checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v284.json`; it records
11,696 functions and 587 remaining default `sub_` names. Its database
SHA-256 is
`75ec25ac82f7724c4332611be2e8bfdea2dc1453f825b0bd0e845e53cfed8bf7`.

## Current Spectron libjpeg color-deconverter labels

The v285 pass translated the six retained function boundaries immediately
before the target `v18_jinit_color_deconverter_jpeg_decompress_struct`
initializer at `0x2ab454`. The corresponding source initializer is at
`0x29dfe4`. The role names follow the [libjpeg-turbo `jdcolor.c` implementation](https://github.com/libjpeg-turbo/libjpeg-turbo/blob/main/src/jdcolor.c), including the conversion methods included from the color-extension helpers.

| 1.8 role | Source | Spectron target | Applied label | Target use | Match |
| --- | ---: | ---: | --- | --- | --- |
| `ycc_rgb_convert` | `0x29da68` | `0x2aaed8` | `v18_jpeg_ycc_rgb_convert` | YCbCr-to-RGB branch at `0x2ab688` | exact metrics |
| `null_convert` | `0x29db28` | `0x2aaf98` | `v18_jpeg_null_convert` | direct component interleave at `0x2ab5d0`, `0x2ab64c`, and `0x2ab664` | exact metrics |
| `gray_rgb_convert` | `0x29dba4` | `0x2ab014` | `v18_jpeg_gray_rgb_convert` | grayscale-to-RGB branch at `0x2ab674` | exact metrics |
| `ycck_cmyk_convert` | `0x29dee8` | `0x2ab358` | `v18_jpeg_ycck_cmyk_convert` | YCCK-to-CMYK branch at `0x2ab780` | exact metrics |
| `start_pass_dcolor` | `0x29dfc8` | `0x2ab438` | `v18_jpeg_start_pass_dcolor` | empty start-pass slot at `0x2ab480` and `0x2ab488` | exact metrics |
| `grayscale_convert` | `0x29dfcc` | `0x2ab43c` | `v18_jpeg_grayscale_convert` | luminance-copy branches at `0x2ab538` and `0x2ab540` | exact metrics |

The YCbCr-to-RGB method uses the precomputed chroma tables and range limit
array to write three interleaved output bytes. The direct-copy method handles
the general component count by walking each input plane and placing samples in
the interleaved destination. The grayscale-to-RGB method repeats each sample
across three channels and keeps the target's ARM64 vectorized wide-row path.
The YCCK-to-CMYK method converts the first three channels, inverts them for
CMYK, and preserves the black channel. The grayscale wrapper copies only the
first input plane through the shared sample-row helper. The empty start-pass
method is intentionally a four-byte no-op.

The target initializer's switch over input and output colorspaces independently
confirms each assignment. It selects the direct-copy method for unchanged
component layouts, the grayscale method for luminance-only output, the
YCbCr-to-RGB and grayscale-to-RGB methods for their corresponding conversions,
and the YCCK-to-CMYK method for four-channel output. This assignment evidence
is stronger than a size-only match because several of the routines are
specialized variants of methods included from `jdcolext.c`.

All six source and target rows are high-confidence matches and all six match
the normalized and complete recorded ARM64 feature set. The source and target
`nullsub_6` start-pass entry is given the semantic label
`v18_jpeg_start_pass_dcolor`; this records the source role while keeping the
IDA view useful.

The machine-readable evidence is
`artifacts/spectron_jpeg_color_deconverter_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_jdcolor_anchors.py`. All six aliases were
applied to a copy of the v284 IDA database and reopened as the v285 database
with zero failures. The v285 checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v285.json`; it records
11,696 functions and 582 remaining default `sub_` names. Its database
SHA-256 is
`9808e39ad80e9bb122ae35656c2eb48d23097bfcdd31e3b67db568d6bc13cd1d`.

## Current Spectron libjpeg main-controller labels

The v288 pass translated the four retained routines around the target
`v18_jinit_d_main_controller_jpeg_decompress_struct_int` initializer at
`0x2ad964`. The corresponding source initializer is at `0x2a04f4`. The role
names follow the [libjpeg-turbo `jdmainct.c` implementation](https://github.com/libjpeg-turbo/libjpeg-turbo/blob/main/src/jdmainct.c), which sits between coefficient decoding and postprocessing.

| 1.8 role | Source | Spectron target | Applied label | Target use | Match |
| --- | ---: | ---: | --- | --- | --- |
| `process_data_simple_main` | `0x29fc98` | `0x2ad108` | `v18_jpeg_process_data_simple_main` | selected without context rows at `0x2ad5f8` | exact metrics |
| `process_data_context_main` | `0x29fd40` | `0x2ad1b0` | `v18_jpeg_process_data_context_main` | selected with context rows at `0x2ad62c` | exact metrics |
| `process_data_crank_post` | `0x2a00c0` | `0x2ad530` | `v18_jpeg_process_data_crank_post` | final two-pass destination path at `0x2ad5c4` | exact metrics |
| `start_pass_main` | `0x2a00f8` | `0x2ad568` | `v18_jpeg_start_pass_main` | public start-pass slot at `0x2ad9a0` | normalized shape |

The simple path fills the main buffer through the coefficient controller,
passes available row groups to postprocessing, and marks the buffer empty
when those rows are consumed. The context path alternates the two funny
pointer workspaces, manufactures the top and bottom context rows required by
the upsampler, and advances the iMCU and row-group state machine. The crank
path is the short final-pass bridge to postprocessing.

The dispatcher selects the crank path for the final two-pass destination mode.
For a normal pass, it selects the context-row path when the upsampler needs
neighboring rows and the simple path otherwise. It also resets the buffer and
row-group state at the start of a pass. This callback placement ties all four
target routines to the source controller contract.

All four source and target rows are high-confidence matches and all four
match normalized ARM64 feature shape. Three also match the complete recorded
feature set; only `start_pass_main` differs in register allocation detail.
Both source and target candidates were default IDA names before this pass, so
the `v18_jpeg_` labels are reviewed semantic roles rather than recovered
debug symbols.

The machine-readable evidence is
`artifacts/spectron_jpeg_main_controller_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_jdmainct_anchors.py`. The aliases were
applied to a copy of the v287 IDA database and reopened as the v288 database
with zero failures. The v288 checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v288.json`; it records
11,696 functions and 575 remaining default `sub_` names. Its database
SHA-256 is
`99d386649d7356bf021dfd87e8e153b29e24a52f14b83504d5b7b28956c68065`.

## Current Spectron libjpeg compressor color-converter labels

The v289 pass translated six retained routines around the target
`v18_jinit_color_converter_jpeg_compress_struct` initializer at `0x2b0354`.
The corresponding source initializer is at `0x2a2ee4`. The role mapping
follows the [libjpeg-turbo `jccolor.c` implementation](https://github.com/libjpeg-turbo/libjpeg-turbo/blob/main/src/jccolor.c).

| 1.8 role | Source | Spectron target | Applied label | Target callback field |
| --- | ---: | ---: | --- | --- |
| `rgb_ycc_start` | `0x2a2b0c` | `0x2aff7c` | `v18_jpeg_rgb_ycc_start` | table initializer at `0x2b0608`, `0x2b0624`, and `0x2b0640` |
| `rgb_ycc_convert` | `0x2a2bd0` | `0x2b0040` | `v18_jpeg_rgb_ycc_convert` | RGB-to-YCbCr branch at `0x2b0630` |
| `rgb_gray_convert` | `0x2a2ca4` | `0x2b0114` | `v18_jpeg_rgb_gray_convert` | RGB-to-grayscale branch at `0x2b064c` |
| `cmyk_ycck_convert` | `0x2a2d1c` | `0x2b018c` | `v18_jpeg_cmyk_ycck_convert` | CMYK-to-YCCK branch at `0x2b0614` |
| compressor `grayscale_convert` | `0x2a2e20` | `0x2b0290` | `v18_jpeg_c_grayscale_convert` | direct grayscale copy at `0x2b05a8` |
| compressor `null_convert` | `0x2a2e6c` | `0x2b02dc` | `v18_jpeg_c_null_convert` | direct multi-component copy at `0x2b0518` |

The target parent assigns the six callbacks in the same colorspace branches as
the source. `rgb_ycc_start` builds the fixed-point lookup regions shared by
the RGB-to-YCbCr and CMYK-to-YCCK paths. The two RGB conversion routines emit
three or one output components respectively. The CMYK routine inverts the
first three inputs, converts them to YCC, and preserves K. The final two rows
are direct copy loops for grayscale or general multi-component input.

All six rows are high-confidence matches with identical complete ARM64
feature metrics. Both builds retained default IDA names before this pass, so
the applied names are reviewed semantic roles rather than recovered debug
symbols. The `c_` qualifiers intentionally distinguish the compressor-side
`jccolor.c` copies from the decompressor-side `jdcolor.c` routines already
named `v18_jpeg_grayscale_convert` and `v18_jpeg_null_convert`.

The machine-readable evidence is
`artifacts/spectron_jpeg_compressor_color_converter_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_jccolor_anchors.py`. All six aliases were
applied to a copy of the v288 IDA database and reopened as the v289 database
with zero failures. The v289 checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v289.json`; it records
11,696 functions and 569 remaining default `sub_` names. Its database
SHA-256 is
`15b16bb239ebe1e4eccc22629d68a31a8275feff61be37b2a408e19db20ea9bb`.

## Current Spectron libjpeg compressor coefficient-controller labels

The v290 pass translated five retained routines around the target
`v18_jinit_c_coef_controller_jpeg_compress_struct_int` initializer at
`0x2afe2c`. The corresponding source initializer is at `0x2a29bc`. The role
mapping follows the callback contract in the [libjpeg-turbo `jccoefct.c` implementation](https://github.com/libjpeg-turbo/libjpeg-turbo/blob/main/src/jccoefct.c).

| 1.8 role | Source | Spectron target | Applied label | Target callback field |
| --- | ---: | ---: | --- | --- |
| `start_iMCU_row` | `0x2a1b50` | `0x2aefc0` | `v18_jpeg_start_iMCU_row` | shared row reset calls at `0x2af270`, `0x2af6a0`, and `0x2afd14` |
| `compress_output` | `0x2a1bb4` | `0x2af024` | `v18_jpeg_compress_output` | crank-destination branch at `0x2af8c8` |
| `compress_data` | `0x2a1e40` | `0x2af2b0` | `v18_jpeg_compress_data` | pass-through branch at `0x2af880` |
| `start_pass_coef` | `0x2a2394` | `0x2af804` | `v18_jpeg_start_pass_coef` | public start-pass field at `0x2afe64` |
| `compress_first_pass` | `0x2a24cc` | `0x2af93c` | `v18_jpeg_compress_first_pass` | full-buffer save-and-pass branch at `0x2afd14` |

The shared row helper resets the vertical offset and MCU counter, then chooses
the number of MCU rows for an interleaved, noninterleaved, or bottom-of-image
row. The pass-through routine runs the forward DCT on input sample planes,
fills right and bottom edge blocks, and sends each MCU to the entropy coder.
The first-pass routine does the same work while storing coefficient rows in
virtual arrays, and the output routine reads those arrays during later passes.

The target `start_pass_coef` dispatcher selects the same three paths as the
source: direct pass-through, full-buffer first pass, and crank-destination
output. All five rows are high-confidence matches with identical normalized
ARM64 feature shape. Four match the complete recorded feature set. The only
exception is register allocation detail in `start_pass_coef`, which is
recorded in the machine-readable artifact. Both builds retained default IDA
names before this pass, so the applied names are reviewed semantic roles.

The machine-readable evidence is
`artifacts/spectron_jpeg_compressor_coefficient_controller_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_jccoefct_anchors.py`. All five aliases
were applied to a copy of the v289 IDA database and reopened as the v290
database with zero failures. The v290 checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v290.json`; it records
11,696 functions and 564 remaining default `sub_` names. Its database
SHA-256 is
`5a74d5d6915311fedf66eb33f1c7a6901baad8ad47653ae1c445698eccbf37e7`.

## Current Spectron GIF LZW line-decoder label

The v296 pass translated one static helper from giflib's `dgif_lib.c` block.
The source helper is the internal `DGifDecompressLine` routine at `0x2acb20`.
The stripped target has the same body at `0x2b9f90`. The target callers are
`v18_DGifGetLine` at `0x2bb6ac` and `v18_DGifGetPixel` at `0x2bb770`, which
matches the source callers at `0x2ae28c` and `0x2ae350`.

| 1.8 role | Source | Spectron target | Applied label | Target callers | Match |
| --- | ---: | ---: | --- | --- | --- |
| `DGifDecompressLine` | `0x2acb20` | `0x2b9f90` | `v18_DGifDecompressLine` | `v18_DGifGetLine`, `v18_DGifGetPixel` | normalized shape |

The helper takes a GIF file state, a destination line buffer, and a pixel
count. It drains pixels already held on the LZW stack, reads compressed
sub-blocks through the configured input callback or `fread`, maintains the
prefix and suffix dictionary, handles clear and end-of-data codes, and writes
decoded pixels to the caller's line. This is the internal line decoder, not
the public `DGifGetLZCodes` API, which is separately present at source
`0x2ae460` and target `0x2bb880`.

The source and target have identical size, instruction count, basic-block
shape, branch and call counts, mnemonic sequence, opcode shape, register
shape, overall shape, and string-reference fields. Only the register-detail
hash differs. The role is also consistent with the giflib
[`dgif_lib.c` implementation](https://android.googlesource.com/platform/external/giflib/%2B/dc07290edccc2c3fc4062da835306f809cea1fdc/dgif_lib.c).

The machine-readable evidence is
`artifacts/spectron_gif_lzw_line_decoder_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_gif_lzw_line_decoder_anchors.py`. The
alias was applied to an isolated copy of v295 and reopened with zero anchor
failures. The broad high-confidence map still verifies all 3,641 earlier
aliases with zero failures. The v296 database contains 11,696 functions and
531 remaining default `sub_` names. Its database SHA-256 is
`12f5465bef23235773b87f7c68339eca35246663316adfc7ae5baf11795b474e`.
The checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v296.json`.

The same boundary review found two IDA phantom functions in the forward-DCT
constant pool. Source `0x2ac400` and target `0x2b9870` each hold 64 bytes of
repeated NEON constants in executable `.text`; the floating-point DCT reaches
them with `ADR` and loads them as vectors. They are immediately followed by
the real integer forward-DCT functions at source `0x2ac440` and target
`0x2b98b0`. No label is assigned to the pools in v296. They are tracked for
a later IDA boundary repair so the function inventory does not count data as
callable code.

The v297 repair removes the target phantom function at `0x2b9870` and defines
the four 16-byte vectors as `xmmword_2B9870`, `xmmword_2B9880`,
`xmmword_2B9890`, and `xmmword_2B98A0`. The real target function
`v18_jpeg_fdct_ifast_int` remains at `0x2b98b0`. The raw 64-byte interval is
unchanged, and the reopened repaired database no longer counts the pool as a
function. The repair artifact is
`artifacts/spectron_fdct_literal_pool_boundary_repair_20260828.json`; the v297
checkpoint records 11,695 functions and 530 remaining default `sub_` names.

## Current Spectron FreeType base cleanup labels

The v298 pass translated two static cleanup helpers in the bundled FreeType
base object implementation. The target functions were still IDA defaults at
the start of the pass. Their source and target pseudocode match, and their
placement beside the existing `FT_Done_Face`, `FT_Done_GlyphSlot`, and
`FT_List_Finalize` routines gives a direct lifecycle context.

| 1.8 role | Source | Spectron target | Applied label | Evidence | Match |
| --- | ---: | ---: | --- | --- | --- |
| `destroy_size` | `0x250e94` | `0x25e304` | `v18_destroy_size` | `FT_List_Finalize` size destructor | exact metrics |
| `destroy_face` | `0x252e90` | `0x260300` | `v18_destroy_face` | face and driver teardown | normalized shape |

`destroy_size` receives the memory allocator, a `FT_Size`, and its driver. It
invokes the client finalizer and driver `done_size` callback, then releases
the size's internal state and the size object. The source helper is passed to
`FT_List_Finalize` by `destroy_face`; the target stores the renamed helper in
the same list-finalization call.

`destroy_face` releases the face's glyph slots, finalizes the sizes list with
`destroy_size`, clears the current size, invokes client and driver callbacks,
closes the stream, frees internal face storage, and finally frees the face.
The target's public `v18_FT_Done_Face` and module-removal paths lead into the
same cleanup block. The target and source have identical normalized feature
metrics. The only difference recorded for `destroy_face` is register-detail
allocation, which is expected across the two builds.

These are reviewed semantic aliases, not claims that the stripped 2.2 ELF
retained the original static names. The machine-readable evidence is
`artifacts/spectron_freetype_base_cleanup_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_freetype_base_cleanup_anchors.py`. Both
aliases were applied to an isolated v297 database copy and reopened with zero
anchor failures. The full regression still verifies all 3,641 earlier
high-confidence aliases. The v298 database contains 11,695 functions and 528
remaining default `sub_` names. Its SHA-256 is
`1de475653381e7ddf9b17cfadf43d816a29babfdb40b87bbf1f7825e0866c26d`.

The source reference is the Android FreeType Donut copy of
[`src/base/ftobjs.c`](https://android.googlesource.com/platform/external/freetype/+/donut-release/src/base/ftobjs.c).
The checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v298.json`.

## Current Spectron FreeType SFNT service labels

The v299 pass translated five callbacks from the FreeType SFNT service
records. The original data records identify the `tt-cmaps`,
`postscript-font-name`, and `sfnt-table` slots. The corresponding target
records sit at `0x371240` and point to the five target functions below.

| 1.8 role | Source | Service slot | Spectron target | Applied label | Match |
| --- | ---: | --- | ---: | --- | --- |
| `tt_get_cmap_info` | `0x254b98` | `tt-cmaps.get_cmap_info` | `0x262008` | `v18_tt_get_cmap_info` | exact metrics |
| `sfnt_get_ps_name` | `0x2579b4` | `postscript-font-name.get_ps_font_name` | `0x264e24` | `v18_sfnt_get_ps_name` | exact metrics |
| `tt_face_load_any` | `0x25663c` | `sfnt-table.load_table` | `0x263aac` | `v18_tt_face_load_any` | exact metrics |
| `get_sfnt_table` | `0x254d80` | `sfnt-table.get_table` | `0x2621f0` | `v18_get_sfnt_table` | normalized shape |
| `sfnt_table_info` | `0x25796c` | `sfnt-table.table_info` | `0x264ddc` | `v18_sfnt_table_info` | exact metrics |

`tt_get_cmap_info` dispatches through the active cmap class. `sfnt_get_ps_name`
returns or constructs the PostScript font name. `tt_face_load_any` resolves a
table directory entry and reads the requested range. `get_sfnt_table` maps a
public table selector to the corresponding face field, while
`sfnt_table_info` exposes the table count and directory metadata. The source
and target functions have identical normalized feature records. Only
`get_sfnt_table` has a recorded register-allocation difference.

These are semantic aliases for a stripped build. They are supported by the
service-slot layout, official FreeType source roles, matching pseudocode, and
reopened IDA evidence. The machine-readable record is
`artifacts/spectron_freetype_sfnt_service_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_freetype_service_anchors.py`. The five
aliases reopened with zero failures, and the full regression still verifies
all 3,641 earlier high-confidence aliases. The v299 database contains 11,695
functions and 523 remaining default `sub_` names. Its SHA-256 is
`f6bcdeba610fbe47ba182477ccc74b5cbff727b17f9b0013395beb3902228367`.

The source references are the Android FreeType SFNT driver,
[`sfdriver.c`](https://android.googlesource.com/platform/external/freetype/+/75b4fbd0462ad4544ffc447213777d1f0d536c1a/src/sfnt/sfdriver.c),
[`ttcmap.c`](https://android.googlesource.com/platform/external/freetype/+/f720f0db/src/sfnt/ttcmap.c),
and [`ttload.c`](https://android.googlesource.com/platform/external/freetype/+/3053d1b9db55099918843889e4809ce97483ca9f/src/sfnt/ttload.c). The
checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v299.json`.

## Current Spectron FreeType SFNT interface labels

The v300 pass translated the 19 non-null callbacks in the target's parallel
`sfnt_interface` record and the two internal name helpers reached by
`sfnt_load_face`. The source record is at `0x36cda0`; the target record is at
`0x37fb70`. The target `load_any` callback at offset `+0x28` was already
translated in v299, so it is intentionally not repeated here.

| 1.8 role | Source | SFNT slot or helper | Spectron target | Applied label | Match |
| --- | ---: | --- | ---: | --- | --- |
| `tt_face_goto_table` | `0x2565e8` | `sfnt_interface.goto_table` | `0x263a58` | `v18_tt_face_goto_table` | exact metrics |
| `sfnt_init_face` | `0x257704` | `sfnt_interface.init_face` | `0x264b74` | `v18_sfnt_init_face` | normalized shape |
| `sfnt_load_face` | `0x25a8e0` | `sfnt_interface.load_face` | `0x267d50` | `v18_sfnt_load_face` | normalized shape |
| `sfnt_done_face` | `0x257254` | `sfnt_interface.done_face` | `0x2646c4` | `v18_sfnt_done_face` | exact metrics |
| `tt_face_load_head` | `0x258204` | `sfnt_interface.load_head` | `0x265674` | `v18_tt_face_load_head` | normalized shape |
| `tt_face_load_hhea` | `0x256d24` | `sfnt_interface.load_hhea` | `0x264194` | `v18_tt_face_load_hhea` | exact metrics |
| `tt_face_load_cmap` | `0x258198` | `sfnt_interface.load_cmap` | `0x265608` | `v18_tt_face_load_cmap` | exact metrics |
| `tt_face_load_maxp` | `0x257f64` | `sfnt_interface.load_maxp` | `0x2653d4` | `v18_tt_face_load_maxp` | exact metrics |
| `tt_face_load_os2` | `0x256b7c` | `sfnt_interface.load_os2` | `0x263fec` | `v18_tt_face_load_os2` | normalized shape |
| `tt_face_load_post` | `0x256b14` | `sfnt_interface.load_post` | `0x263f84` | `v18_tt_face_load_post` | normalized shape |
| `tt_face_load_name` | `0x256960` | `sfnt_interface.load_name` | `0x263dd0` | `v18_tt_face_load_name` | normalized shape |
| `tt_face_free_name` | `0x255fc0` | `sfnt_interface.free_name` | `0x263430` | `v18_tt_face_free_name` | exact metrics |
| `tt_face_load_kern` | `0x257030` | `sfnt_interface.load_kern` | `0x2644a0` | `v18_tt_face_load_kern` | exact metrics |
| `tt_face_load_gasp` | `0x256ef4` | `sfnt_interface.load_gasp` | `0x264364` | `v18_tt_face_load_gasp` | exact metrics |
| `tt_face_load_pclt` | `0x2568fc` | `sfnt_interface.load_pclt` | `0x263d6c` | `v18_tt_face_load_pclt` | normalized shape |
| `tt_face_get_kerning` | `0x254bb8` | `sfnt_interface.get_kerning` | `0x262028` | `v18_tt_face_get_kerning` | exact metrics |
| `tt_face_load_font_dir` | `0x257c28` | `sfnt_interface.load_font_dir` | `0x265098` | `v18_tt_face_load_font_dir` | normalized shape |
| `tt_face_load_hmtx` | `0x25687c` | `sfnt_interface.load_hmtx` | `0x263cec` | `v18_tt_face_load_hmtx` | exact metrics |
| `tt_face_get_metrics` | `0x2566d8` | `sfnt_interface.get_metrics` | `0x263b48` | `v18_tt_face_get_metrics` | exact metrics |
| `tt_name_entry_ascii_from_other` | `0x256060` | `sfnt_load_face` name helper | `0x2634d0` | `v18_tt_name_entry_ascii_from_other` | exact metrics |
| `tt_name_entry_ascii_from_utf16` | `0x2563d0` | `sfnt_load_face` name helper | `0x263840` | `v18_tt_name_entry_ascii_from_utf16` | exact metrics |

The table corrects an easy-to-make assignment mistake. Source `0x254bb8`
does not implement `tt_face_lookup_table`; the source and target dispatch
records place it in the `get_kerning` slot, and the body consumes a glyph pair
and searches the loaded kern subtables. The table-directory lookup is
`tt_face_goto_table` at source `0x2565e8` and target `0x263a58`.

The source and target functions all have identical normalized feature records.
Thirteen pairs also match the complete recorded metrics; eight differ only in
the register-detail hash. The name helpers preserve the old FreeType roles:
one copies non-Unicode name bytes and the other converts UTF-16BE records to
printable ASCII. These are semantic aliases for the stripped build, not claims
that the original static names survived in the 2.2 ELF.

The source references are the Android FreeType
[`sfnt.h`](https://android.googlesource.com/platform/external/freetype/+/75b4fbd0462ad4544ffc447213777d1f0d536c1a/include/freetype/internal/sfnt.h),
[`sfdriver.c`](https://android.googlesource.com/platform/external/freetype/+/75b4fbd0462ad4544ffc447213777d1f0d536c1a/src/sfnt/sfdriver.c),
[`ttload.c`](https://android.googlesource.com/platform/external/freetype/+/3053d1b9db55099918843889e4809ce97483ca9f/src/sfnt/ttload.c),
the older [`sfobjs.c`](https://android.googlesource.com/platform/external/freetype/+/41371e1e39c8528eb0c4bc40683c736e6683e60c/src/sfnt/sfobjs.c),
[`ttkern.c`](https://android.googlesource.com/platform/external/freetype/+/75b4fbd0462ad4544ffc447213777d1f0d536c1a/src/sfnt/ttkern.c),
and [`ttmtx.c`](https://android.googlesource.com/platform/external/freetype/+/75b4fbd0462ad4544ffc447213777d1f0d536c1a/src/sfnt/ttmtx.c).
The machine-readable evidence is
`artifacts/spectron_freetype_sfnt_interface_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_freetype_interface_anchors.py`. All 21
labels reopened with zero failures. The v300 database contains 11,695
functions and 502 remaining default `sub_` names. Its SHA-256 is
`d05ecd64d3430bfa54cb34120ab750a46f0d8a5dbad54180afd0da18b8e312b5`.

The checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v300.json`.

## Current Spectron FreeType smooth renderer labels

The v301 pass translates the callback-backed FreeType smooth renderer and gray
raster block. The original renderer records are `ft_smooth_lcdv_renderer_class`
at `0x36d1c8`, `ft_smooth_lcd_renderer_class` at `0x36d240`, and
`ft_smooth_renderer_class` at `0x36d2b8`. The stripped target retains parallel
records named `ft_smooth_lcdv_renderer_class1` at `0x37ff98`,
`ft_smooth_lcd_renderer_class1` at `0x380010`, and
`ft_smooth_renderer_class1` at `0x380088`. The shared gray raster records are
at source `0x35e518` and target `0x371298`.

| 1.8 role | Source | Callback record or call site | Spectron target | Applied label | Match |
| --- | ---: | --- | ---: | --- | --- |
| `ft_smooth_init` | `0x25b5f4` | renderer `module_init` | `0x268a64` | `v18_ft_smooth_init` | exact metrics |
| `ft_smooth_set_mode` | `0x25b62c` | renderer `set_mode` | `0x268a9c` | `v18_ft_smooth_set_mode` | exact metrics |
| `gray_raster_done` | `0x25b654` | `ft_grays_raster.raster_done` | `0x268ac4` | `v18_gray_raster_done` | exact metrics |
| `gray_raster_new` | `0x25b76c` | `ft_grays_raster.raster_new` | `0x268bdc` | `v18_gray_raster_new` | exact metrics |
| `ft_smooth_get_cbox` | `0x25b7b4` | renderer `get_glyph_cbox` | `0x268c24` | `v18_ft_smooth_get_cbox` | exact metrics |
| `ft_smooth_render_lcd_v` | `0x25b7dc` | vertical LCD `render_glyph` | `0x268c4c` | `v18_ft_smooth_render_lcd_v` | exact metrics |
| `gray_raster_reset` | `0x25ba90` | `ft_grays_raster.raster_reset` | `0x268f00` | `v18_gray_raster_reset` | exact metrics |
| `ft_smooth_transform` | `0x25baec` | renderer `transform_glyph` | `0x268f5c` | `v18_ft_smooth_transform` | exact metrics |
| `gray_raster_render` | `0x25c878` | `ft_grays_raster.raster_render` | `0x269ce8` | `v18_gray_raster_render` | normalized shape |
| `ft_smooth_render` | `0x25ca78` | normal `render_glyph` | `0x269ee8` | `v18_ft_smooth_render` | exact metrics |
| `ft_smooth_render_lcd` | `0x25ccb8` | horizontal LCD `render_glyph` | `0x26a128` | `v18_ft_smooth_render_lcd` | exact metrics |
| `tt_face_build_cmaps` | `0x2573b8` | called by `sfnt_load_face` | `0x264828` | `v18_tt_face_build_cmaps` | normalized shape |

The first four renderer callbacks are shared across the three records. Their
render slots are the discriminating fields: normal grayscale, horizontal LCD,
and vertical LCD. The gray raster callbacks provide the constructor, pool
reset, scan conversion, and destructor used by each renderer. The cmap helper
is nearby in the SFNT loader rather than in the smooth module, but its body is
an exact source-to-target shape match after register details are normalized.

The callback-table evidence and the official FreeType source resolve these
roles without relying on address arithmetic alone. Ten rows match the complete
feature record. `gray_raster_render` and `tt_face_build_cmaps` differ only in
register allocation detail, and all 12 match normalized shape. These are
semantic aliases for the stripped build, not claims that the original static
names survived in the target ELF.

The machine-readable evidence is
`artifacts/spectron_freetype_smooth_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_freetype_smooth_anchors.py`. All 12
aliases reopened with zero failures, and the full regression still verifies
all 3,641 earlier high-confidence aliases. The v301 database contains 11,695
functions and 490 remaining default `sub_` names. Its SHA-256 is
`385ff0bf22b942386ac6857321529201857ac190668dca6c7e673107be8350ca`.

The source references are the Android FreeType
[`ftsmooth.c`](https://android.googlesource.com/platform/external/freetype/+/2689da543c08133100124cab3ab19523b04f2f3d/src/smooth/ftsmooth.c),
[`ftgrays.c`](https://android.googlesource.com/platform/external/freetype/+/2689da543c08133100124cab3ab19523b04f2f3d/src/smooth/ftgrays.c),
and [`ttcmap.c`](https://android.googlesource.com/platform/external/freetype/+/f720f0db/src/sfnt/ttcmap.c).
The checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v301.json`.

## Current Spectron FreeType gray-raster internal labels

The v302 pass translates nine internal helpers from the FreeType gray raster
worker. The original outline callback table is at `0x35e4e8`, with the move,
line, conic, and cubic callbacks in its four consecutive slots. The raster
record is `ft_grays_raster` at `0x35e518`; its target counterpart is
`ft_grays_raster1` at `0x371298`. The target functions were still IDA default
names before this pass.

| 1.8 role | Source | Callback or call topology | Spectron target | Applied label | Match |
| --- | ---: | --- | ---: | --- | --- |
| `gray_render_span` | `0x25b660` | bitmap or direct-span output from `gray_raster_render` | `0x268ad0` | `v18_gray_render_span` | normalized shape |
| `gray_convert_glyph_inner` | `0x25bb64` | band helper called by `gray_convert_glyph` | `0x268fd4` | `v18_gray_convert_glyph_inner` | normalized shape |
| `gray_move_to` | `0x25bca8` | outline table `0x35e4e8 + 0x00` | `0x269118` | `v18_gray_move_to` | exact metrics |
| `gray_convert_glyph` | `0x25be44` | band loop called by `gray_raster_render` | `0x2692b4` | `v18_gray_convert_glyph` | exact metrics |
| `gray_render_scanline` | `0x25cf78` | scanline segment called by `gray_render_line` | `0x26a3e8` | `v18_gray_render_scanline` | exact metrics |
| `gray_render_line` | `0x25d4bc` | reached by line and curve callbacks | `0x26a92c` | `v18_gray_render_line` | exact metrics |
| `gray_render_cubic` | `0x25dcbc` | outline table `0x35e4e8 + 0x18` | `0x26b12c` | `v18_gray_render_cubic` | exact metrics |
| `gray_render_conic` | `0x25e04c` | outline table `0x35e4e8 + 0x10` | `0x26b4bc` | `v18_gray_render_conic` | exact metrics |
| `gray_line_to` | `0x25e2ec` | outline table `0x35e4e8 + 0x08` | `0x26b75c` | `v18_gray_line_to` | exact metrics |

The four outline callbacks form the `FT_Outline_Decompose` entry points. The
move callback starts a contour, the line callback sends a straight edge to
the line renderer, and the conic and cubic callbacks subdivide curves into
line segments. `gray_convert_glyph` bounds the work into bands and calls
`gray_convert_glyph_inner`, which decomposes the outline and records cells.
`gray_render_line` walks those edges across scanlines, and
`gray_render_scanline` updates the fixed-point cell coverage. The span helper
writes the resulting coverage into bitmap rows or forwards it to the direct
span callback.

The original and target pseudocode have the same worker state, callback
topology, and operation. All nine pairs match normalized ARM64 feature shape.
Seven also match the complete recorded feature set. `gray_render_span` and
`gray_convert_glyph_inner` differ only in the register-detail hash. These are
semantic aliases for the stripped build, not claims that the original static
names survived in the target ELF.

The machine-readable evidence is
`artifacts/spectron_freetype_gray_internal_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_freetype_gray_internal_anchors.py`. All
nine aliases reopened with zero failures, and the full regression still
verifies all 3,641 earlier high-confidence aliases. The v302 database contains
11,695 functions and 481 remaining default `sub_` names. Its SHA-256 is
`71ab01a1215b46848c4ae511fbd065432be83d357d80a2f17a1efa045d814ab5`.

The source references are the Android FreeType
[`ftgrays.c`](https://android.googlesource.com/platform/external/freetype/+/2689da543c08133100124cab3ab19523b04f2f3d/src/smooth/ftgrays.c)
and [`ftimage.h`](https://android.googlesource.com/platform/external/freetype/+/8483e21a1fdc252bd234eb55c6b63c17551933ee/include/freetype/ftimage.h).
The checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v302.json`.

## Current Spectron FreeType TrueType runtime labels

The v304 pass translates six runtime helpers in the stripped TrueType block.
The target addresses are the source addresses plus `0xd470`, matching the
neighboring SFNT, renderer, and interpreter passes. These are semantic aliases
for the stripped build, not claims that original debug symbols survived in the
target ELF.

| 1.8 role | Source | Spectron target | Applied label | Match |
| --- | ---: | ---: | --- | --- |
| `Direct_Move_Orig` | `0x25ec84` | `0x26c0f4` | `v18_Direct_Move_Orig` | exact metrics |
| `Direct_Move` | `0x25ed14` | `0x26c184` | `v18_Direct_Move` | exact metrics |
| `tt_slot_init` | `0x25f4f4` | `0x26c964` | `v18_tt_slot_init` | exact metrics |
| `tt_face_done` | `0x25f500` | `0x26c970` | `v18_tt_face_done` | exact metrics |
| `tt_face_init` | `0x25f648` | `0x26cab8` | `v18_tt_face_init` | normalized shape, register-detail difference |
| `Current_Ratio` | `0x25fd8c` | `0x26d1fc` | `v18_Current_Ratio` | exact metrics |

`Direct_Move_Orig` writes scaled movement into the interpreter's original
point zone. `Direct_Move` writes into the current point zone and sets the
corresponding touch tags. `tt_slot_init` creates the extra glyph-loader
storage. `tt_face_init` and `tt_face_done` bracket the SFNT and TrueType table
resources, including the loca frame and bytecode programs. `Current_Ratio`
derives the effective scaling ratio from face metrics and vector components
and caches it in the execution context.

The machine-readable evidence is
`artifacts/spectron_freetype_tt_runtime_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_freetype_tt_runtime_anchors.py`. All six
aliases reopened with zero failures, and the full regression still verifies
all 3,641 earlier high-confidence aliases. The v304 database contains 11,695
functions and 456 remaining default `sub_` names. Its SHA-256 is
`8c2e1b1591fbb80bb3d874c3dfa4708d6e7d4bfc503748a70c519f07202494c4`.

The source references are the Android FreeType
[`ttobjs.c`](https://android.googlesource.com/platform/external/freetype/+/3053d1b9db55099918843889e4809ce97483ca9f/src/truetype/ttobjs.c)
and [`ttinterp.c`](https://android.googlesource.com/platform/external/freetype/+/2689da543c08133100124cab3ab19523b04f2f3d/src/truetype/ttinterp.c).
The checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v304.json`.

## Current Spectron FreeType TrueType rounding labels

The v305 pass translates the seven remaining rounding callbacks in the
TrueType interpreter's `Round_*` family, plus `Compute_Funcs`. The target
addresses are the source addresses plus `0xd470`, and the target pseudocode
retains the same arithmetic and callback selection. These are semantic aliases
for the stripped build, not claims that original debug symbols survived in the
target ELF.

| 1.8 role | Source | Spectron target | Applied label | Match |
| --- | ---: | ---: | --- | --- |
| `Round_To_Grid` | `0x25fe38` | `0x26d2a8` | `v18_Round_To_Grid` | exact metrics |
| `Round_To_Half_Grid` | `0x25fe7c` | `0x26d2ec` | `v18_Round_To_Half_Grid` | exact metrics |
| `Round_Down_To_Grid` | `0x25feb8` | `0x26d328` | `v18_Round_Down_To_Grid` | exact metrics |
| `Round_Up_To_Grid` | `0x25fef4` | `0x26d364` | `v18_Round_Up_To_Grid` | exact metrics |
| `Round_To_Double_Grid` | `0x25ff38` | `0x26d3a8` | `v18_Round_To_Double_Grid` | exact metrics |
| `Round_Super` | `0x25ff7c` | `0x26d3ec` | `v18_Round_Super` | exact metrics |
| `Round_Super_45` | `0x25ffe8` | `0x26d458` | `v18_Round_Super_45` | exact metrics |
| `Compute_Funcs` | `0x260050` | `0x26d4c0` | `v18_Compute_Funcs` | normalized shape, register-detail difference |

The seven `Round_*` callbacks implement the grid modes selected by
`Compute_Round`, including the period-based `Round_Super` variants.
`Compute_Funcs` installs the projection and movement callbacks for the active
graphics-state vectors. Seven pairs match the complete recorded feature set;
`Compute_Funcs` differs only in the register-detail hash. All eight match
normalized ARM64 shape and all eight aliases reopened with zero failures. The
full regression still verifies all 3,641 earlier high-confidence aliases. The
v305 database contains 11,695 functions and 448 remaining default `sub_`
names. Its SHA-256 is
`28920bb7cd08c4b94bc16b82bd3a4770e9873b55af3ff2269bec87755876c931`.

The machine-readable evidence is
`artifacts/spectron_freetype_tt_rounding_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_freetype_tt_rounding_anchors.py`. The
source reference is the Android FreeType
[`ttinterp.c`](https://android.googlesource.com/platform/external/freetype/+/f720f0dbcf012d6c984dbbefa0875ef9840458c6/src/truetype/ttinterp.c).
The checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v305.json`.

## Current Spectron FreeType TrueType opcode-state labels

The v306 pass translates six small handlers in the stripped TrueType
interpreter. The source and target functions are connected by the parallel
`TT_RunIns` opcode dispatcher and the same `0xd470` displacement. These are
semantic aliases for the stripped build, not claims that original debug
symbols survived in the target ELF.

| 1.8 role | Source | Spectron target | Applied label | Match |
| --- | ---: | ---: | --- | --- |
| `Ins_SZP0` | `0x2602a4` | `0x26d714` | `v18_Ins_SZP0` | exact metrics |
| `Ins_SZP1` | `0x2602fc` | `0x26d76c` | `v18_Ins_SZP1` | exact metrics |
| `Ins_SZP2` | `0x260354` | `0x26d7c4` | `v18_Ins_SZP2` | exact metrics |
| `Ins_SZPS` | `0x2603ac` | `0x26d81c` | `v18_Ins_SZPS` | exact metrics |
| `Ins_ALIGNRP` | `0x260468` | `0x26d8d8` | `v18_Ins_ALIGNRP` | exact metrics |
| `Ins_UTP` | `0x260590` | `0x26da00` | `v18_Ins_UTP` | exact metrics |

The `SZP` family selects the twilight or glyph zone for the interpreter's
zone pointers and updates the matching graphics-state selector. `AlignRP`
projects the reference point and applies the movement callback to the
selected points. `UTP` clears touch bits when the free vector permits it.
All six pairs match the complete recorded feature set, and all six aliases
reopened with zero failures. The v306 database contains 11,695 functions and
442 remaining default `sub_` names. Its SHA-256 is
`4a4eb58f2245daf73e262e87c447370e9e8da96329e5a0ab19ca8e2740ad91df`.

The machine-readable evidence is
`artifacts/spectron_freetype_tt_opcode_state_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_freetype_tt_opcode_state_anchors.py`.
The source reference is the Android FreeType
[`ttinterp.c`](https://android.googlesource.com/platform/external/freetype/+/f720f0dbcf012d6c984dbbefa0875ef9840458c6/src/truetype/ttinterp.c).
The checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v306.json`.

## Current Spectron FreeType TrueType opcode-core labels

The v307 pass translates six core helpers and opcode handlers in the stripped
TrueType block. The source and target functions share the same `0xd470`
displacement. These are semantic aliases for the stripped build, not claims
that original debug symbols survived in the target ELF.

| 1.8 role | Source | Spectron target | Applied label | Match |
| --- | ---: | ---: | --- | --- |
| `Ins_MDRP` | `0x260660` | `0x26dad0` | `v18_Ins_MDRP` | exact metrics |
| `Ins_MIRP` | `0x2608e0` | `0x26dd50` | `v18_Ins_MIRP` | exact metrics |
| `Normalize` | `0x260bc4` | `0x26e034` | `v18_Normalize` | exact metrics |
| `Ins_MINDEX` | `0x260d7c` | `0x26e1ec` | `v18_Ins_MINDEX` | exact metrics |
| `TT_Done_Context` | `0x260e00` | `0x26e270` | `v18_TT_Done_Context` | exact metrics |
| `Ins_IP` | `0x260e8c` | `0x26e2fc` | `v18_Ins_IP` | exact metrics |

`MDRP` applies the opcode-selected rounding and minimum-distance rules before
moving a point. `MIRP` obtains its distance from the control value table.
`Normalize` produces the F2Dot14 unit vectors used by vector-setting paths.
`MINDEX` reorders the interpreter stack, `TT_Done_Context` frees the context
storage, and `IP` interpolates untouched points across a contour. All six
pairs match the complete recorded feature set, and all six aliases reopened
with zero failures. The v307 database contains 11,695 functions and 436
remaining default `sub_` names. Its SHA-256 is
`2f9136831860bd73c73b966212134aea033a819d9f96520f6a0d887158f36b9c`.

The machine-readable evidence is
`artifacts/spectron_freetype_tt_opcode_core_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_freetype_tt_opcode_core_anchors.py`.
The source reference is the Android FreeType
[`ttinterp.c`](https://android.googlesource.com/platform/external/freetype/+/f720f0dbcf012d6c984dbbefa0875ef9840458c6/src/truetype/ttinterp.c).
The checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v307.json`.

## Current Spectron FreeType TrueType runtime-tail labels

The v308 pass translates eleven more default target names in the stripped
TrueType block. The source and target addresses again differ by `0xd470`.
These are semantic aliases for the stripped build, not claims that original
debug symbols survived in the target ELF.

| 1.8 role | Source | Spectron target | Applied label | Match |
| --- | ---: | ---: | --- | --- |
| `Ins_ENDF` | `0x261624` | `0x26ea94` | `v18_Ins_ENDF` | exact metrics |
| `tt_size_done` | `0x2616e0` | `0x26eb50` | `v18_tt_size_done` | exact metrics |
| `Dual_Project` | `0x261818` | `0x26ec88` | `v18_Dual_Project` | exact metrics |
| `Ins_FDEF` | `0x2618a4` | `0x26ed14` | `v18_Ins_FDEF` | register-detail difference |
| `Ins_IDEF` | `0x2619d4` | `0x26ee44` | `v18_Ins_IDEF` | register-detail difference |
| `Ins_DELTAP` | `0x261d8c` | `0x26f1fc` | `v18_Ins_DELTAP` | exact metrics |
| `Ins_DELTAC` | `0x261fc4` | `0x26f434` | `v18_Ins_DELTAC` | exact metrics |
| `TT_Load_Context` | `0x2621f4` | `0x26f664` | `v18_TT_Load_Context` | exact metrics |
| `Ins_SHC` | `0x2625e8` | `0x26fa58` | `v18_Ins_SHC` | exact metrics |
| `Ins_SHP` | `0x262864` | `0x26fcd4` | `v18_Ins_SHP` | exact metrics |
| `Ins_ISECT` | `0x262a74` | `0x26fee4` | `v18_Ins_ISECT` | exact metrics |

The opcode rows are tied to the parallel `TT_RunIns` dispatcher cases. `ENDF`
restores the enclosing bytecode range, `FDEF` and `IDEF` register definitions,
the `DELTA*` handlers apply scaled adjustments, and `SHC`, `SHP`, and `ISECT`
move or construct outline points. `tt_size_done` releases size-owned
interpreter state, `Dual_Project` is selected by `Compute_Funcs` for the
dual-vector callback, and `TT_Load_Context` prepares the execution context.
All eleven pairs match normalized ARM64 shape. Nine match the complete
recorded feature set, while `FDEF` and `IDEF` differ only in register-detail
hash. All eleven aliases reopened with zero failures. The v308 database
contains 11,695 functions and 425 remaining default `sub_` names.

The machine-readable evidence is
`artifacts/spectron_freetype_tt_runtime_tail_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_freetype_tt_runtime_tail_anchors.py`.
The source references are Android FreeType's
[`ttinterp.c`](https://android.googlesource.com/platform/external/freetype/+/f720f0dbcf012d6c984dbbefa0875ef9840458c6/src/truetype/ttinterp.c)
and
[`ttobjs.c`](https://android.googlesource.com/platform/external/freetype/+/3053d1b9db55099918843889e4809ce97483ca9f/src/truetype/ttobjs.c).
The checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v308.json`.

### Projection-label correction

The same pass corrects target `0x26bab0` from `v18_TT_DotFix14` to
`v18_Project`. Caller and callback-table review showed that the corresponding
source helper at `0x25e640` is installed by `Compute_Funcs` as `func_project`.
The helper contains the dot-product arithmetic associated with `TT_DotFix14`,
but the callback role is `Project`. The correction is recorded separately in
`artifacts/spectron_freetype_tt_projection_name_correction_20260828.json` so
the historical v307 artifact remains reproducible.

## Current Spectron FreeType TrueType glyph-loader labels

The v309 pass translates seven more default target names in the same
`0xd470`-displaced TrueType region. These are semantic aliases for the
stripped build, supported by matching pseudocode, caller or callback topology,
and the recorded ARM64 feature metrics.

| 1.8 role | Source | Spectron target | Applied label | Match |
| --- | ---: | ---: | --- | --- |
| `load_truetype_glyph` | `0x262db4` | `0x270224` | `v18_load_truetype_glyph` | exact metrics |
| `TT_Load_Glyph` | `0x263d1c` | `0x27118c` | `v18_TT_Load_Glyph` | register-detail difference |
| `tt_glyph_load` | `0x264f78` | `0x2723e8` | `v18_tt_glyph_load` | exact metrics |
| `Ins_SxVTL` | `0x264fcc` | `0x27243c` | `v18_Ins_SxVTL` | exact metrics |
| `Ins_CALL` | `0x26521c` | `0x27268c` | `v18_Ins_CALL` | exact metrics |
| `Ins_LOOPCALL` | `0x265370` | `0x2727e0` | `v18_Ins_LOOPCALL` | exact metrics |
| `Ins_UNKNOWN` | `0x2654d4` | `0x272944` | `v18_Ins_UNKNOWN` | exact metrics |

`load_truetype_glyph` is the recursive helper used for simple and composite
glyphs. `TT_Load_Glyph` owns the loader setup, glyph-slot transfer, and final
metrics, while `tt_glyph_load` is the driver callback that validates the
request and forwards it. `Ins_SxVTL` constructs the vector for the SPVTL and
SFVTL opcode pairs. `Ins_CALL` and `Ins_LOOPCALL` enter defined function code
ranges, and `Ins_UNKNOWN` looks up active IDEF definitions when normal opcode
dispatch does not recognize the byte.

All seven pairs match normalized ARM64 shape. Six match the complete feature
set, while `TT_Load_Glyph` differs only in register-detail hash. All seven
aliases reopened with zero failures. The v309 database contains 11,695
functions and 418 remaining default `sub_` names.

The machine-readable evidence is in
`artifacts/spectron_freetype_tt_glyph_loader_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_freetype_tt_glyph_loader_anchors.py`.
The source references are Android FreeType's
[`ttgload.c`](https://android.googlesource.com/platform/external/freetype/+/2689da543c08133100124cab3ab19523b04f2f3d/src/truetype/ttgload.c),
[`ttdriver.c`](https://android.googlesource.com/platform/external/freetype/+/2689da543c08133100124cab3ab19523b04f2f3d/src/truetype/ttdriver.c),
and
[`ttinterp.c`](https://android.googlesource.com/platform/external/freetype/+/aeb407daf3711a10a27f3bc2223c5eb05158076e/src/truetype/ttinterp.c).
The checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v309.json`.

## Current Spectron FreeType autofit labels

The v310 pass translates eight more default names from the stripped FreeType
autofit block. The source and target functions are separated by the same
`0xd470` displacement used by the adjacent TrueType work. Seven names are
selected by the parallel dummy, Latin, Latin2, CJK, and TrueType module class
records. The eighth is the Latin2 segment-analysis helper reached from both
the metrics-width probe and the hint-application path.

| 1.8 role | Source | Spectron target | Applied label | Callback or call-site evidence | Match |
| --- | ---: | ---: | --- | --- | --- |
| `tt_driver_init` | `0x267ecc` | `0x27533c` | `v18_tt_driver_init` | TrueType module class initializer | exact metrics |
| `af_dummy_hints_init` | `0x267ef0` | `0x275360` | `v18_af_dummy_hints_init` | dummy script class `hints_init` | exact metrics |
| `af_dummy_hints_apply` | `0x267f08` | `0x275378` | `v18_af_dummy_hints_apply` | dummy script class `hints_apply` | exact metrics |
| `af_latin_hints_init` | `0x267f10` | `0x275380` | `v18_af_latin_hints_init` | Latin script class `hints_init` | exact metrics |
| `af_latin2_hints_init` | `0x267f90` | `0x275400` | `v18_af_latin2_hints_init` | Latin2 script class `hints_init` | exact metrics |
| `af_cjk_metrics_scale` | `0x268010` | `0x275480` | `v18_af_cjk_metrics_scale` | CJK script class `metrics_scale` | exact metrics |
| `af_cjk_hints_init` | `0x268050` | `0x2754c0` | `v18_af_cjk_hints_init` | CJK script class `hints_init` | exact metrics |
| `af_latin2_hints_compute_segments` | `0x2680c0` | `0x275530` | `v18_af_latin2_hints_compute_segments` | Latin2 metrics and apply callers | exact metrics |

The class records disambiguate the short initializer bodies that otherwise
look interchangeable in isolation. The CJK record selects the scaling helper
and hint initializer, the Latin and Latin2 records select their own hint
initializers, and the dummy record selects the no-specialized-hinting pair.
`tt_driver_init` creates the TrueType execution context and reports the
FreeType context error when allocation fails. The Latin2 segment helper grows
the per-axis segment array and classifies outline segments before the hinting
stages use them.

All eight pairs match the complete recorded ARM64 feature set. All eight names
reopened with zero failures. The v310 database contains 11,695 functions and
410 remaining default `sub_` names. The machine-readable evidence is
`artifacts/spectron_freetype_autofit_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_freetype_autofit_anchors.py`. The source
role references are Android FreeType's
[`ttobjs.c`](https://android.googlesource.com/platform/external/freetype/+/2689da543c08133100124cab3ab19523b04f2f3d/src/truetype/ttobjs.c),
[`afdummy.c`](https://android.googlesource.com/platform/external/freetype/+/f128616796ff176d99eb03c948fadf68161d5855/src/autofit/afdummy.c),
[`aflatin.c`](https://android.googlesource.com/platform/external/freetype/+/2689da543c08133100124cab3ab19523b04f2f3d/src/autofit/aflatin.c),
[`aflatin2.c`](https://android.googlesource.com/platform/external/freetype/+/6da2e02232e1bcf31cfb78894d46c7902b90ee9/src/autofit/aflatin2.c),
and
[`afcjk.c`](https://android.googlesource.com/platform/external/freetype/+/2689da543c08133100124cab3ab19523b04f2f3d/src/autofit/afcjk.c).
The checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v310.json`.

## Current Spectron FreeType autofit follow-up labels

The v311 pass translates seven more default names immediately after the v310
autofit block. The source and target addresses retain the `0xd470`
displacement. The class callbacks are resolved from the Latin2 and Latin
metrics records, while the remaining helpers are resolved from their caller
graphs and matching FreeType implementations.

| 1.8 role | Source | Spectron target | Applied label | Callback or call-site evidence | Match |
| --- | ---: | ---: | --- | --- | --- |
| `af_latin2_hints_link_segments` | `0x268608` | `0x275a78` | `v18_af_latin2_hints_link_segments` | Latin2 hint-application call | exact metrics |
| `af_latin2_hints_compute_edges` | `0x2688fc` | `0x275d6c` | `v18_af_latin2_hints_compute_edges` | Latin2 hint-application call | exact metrics |
| `af_glyph_hints_done` | `0x268e58` | `0x2762c8` | `v18_af_glyph_hints_done` | autofitter load-glyph cleanup | register-detail difference |
| `af_loader_load_g` | `0x268f44` | `0x2763b4` | `v18_af_loader_load_g` | recursive composite-glyph loader | exact metrics |
| `af_glyph_hints_reload` | `0x2696d4` | `0x276b44` | `v18_af_glyph_hints_reload` | shared outline reload calls | exact metrics |
| `af_latin2_metrics_scale` | `0x269bf4` | `0x277064` | `v18_af_latin2_metrics_scale` | Latin2 script class `metrics_scale` | exact metrics |
| `af_latin_metrics_scale` | `0x269f1c` | `0x27738c` | `v18_af_latin_metrics_scale` | Latin script class `metrics_scale` | exact metrics |

The first two historical labels in this table were later corrected to CJK
roles after the source and target class records were checked. The v311
artifact remains unchanged for auditability, while the current v312 database
uses the corrected names documented below.

`af_latin2_hints_link_segments` scores compatible segments and records
reciprocal stem links or serif relationships. The following
`af_latin2_hints_compute_edges` helper turns those segments into edge records
used by the Latin2 hinting stages. `af_glyph_hints_done` releases the temporary
hint buffers after a glyph pass, `af_glyph_hints_reload` rebuilds the point and
contour representation from an `FT_Outline`, and `af_loader_load_g` handles
transforms and recursive composite glyph components. The last two rows are
the style-metrics scaling callbacks selected by the Latin2 and Latin script
class records.

All seven pairs match normalized ARM64 shape. Six match the complete recorded
feature set, while `af_glyph_hints_done` differs only in register-detail hash.
All seven aliases reopened with zero failures. The v311 database contains
11,695 functions and 403 remaining default `sub_` names. The machine-readable
evidence is
`artifacts/spectron_freetype_autofit_followup_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_freetype_autofit_followup_anchors.py`.
The source role references are Android FreeType's
[`aflatin2.c`](https://android.googlesource.com/platform/external/freetype/+/6da2e02232e1bcf31cfb78894d46c7902b90ee9/src/autofit/aflatin2.c),
[`afhints.c`](https://android.googlesource.com/platform/external/freetype/+/f720f0db/src/autofit/afhints.c),
[`afloader.c`](https://android.googlesource.com/platform/external/freetype/+/f128616796ff176d99eb03c948fadf68161d5855/src/autofit/afloader.c),
and
[`aflatin.c`](https://android.googlesource.com/platform/external/freetype/+/2689da543c08133100124cab3ab19523b04f2f3d/src/autofit/aflatin.c).
The checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v311.json`.

## Current Spectron FreeType autofit metrics and correction labels

The v312 pass adds eleven new labels to the adjacent autofit region and
corrects the first two historical v311 labels. The source and target class
records both select the CJK hint-application callback, proving that the
corrected helpers are CJK roles rather than Latin2 roles.

| 1.8 role | Source | Spectron target | Applied label | Callback or call-site evidence | Match |
| --- | ---: | ---: | --- | --- | --- |
| `af_cjk_hints_link_segments` | `0x268608` | `0x275a78` | `v18_af_cjk_hints_link_segments` | CJK hint-application path; correction of v311 | exact metrics |
| `af_cjk_hints_compute_edges` | `0x2688fc` | `0x275d6c` | `v18_af_cjk_hints_compute_edges` | CJK hint-application path; correction of v311 | exact metrics |
| `af_latin_hints_compute_segments` | `0x26a3d0` | `0x277840` | `v18_af_latin_hints_compute_segments` | shared metrics and hint callers | exact metrics |
| `af_latin_metrics_init_widths` | `0x26a904` | `0x277d74` | `v18_af_latin_metrics_init_widths` | shared Latin and CJK metrics probe | exact metrics |
| `af_cjk_metrics_init` | `0x26adcc` | `0x27823c` | `v18_af_cjk_metrics_init` | CJK script class `metrics_init` | exact metrics |
| `af_hint_normal_stem` | `0x26ae34` | `0x2782a4` | `v18_af_hint_normal_stem` | two calls from CJK apply | exact metrics |
| `af_latin2_metrics_init_widths` | `0x26b198` | `0x278608` | `v18_af_latin2_metrics_init_widths` | Latin2 metrics initializer | exact metrics |
| `af_latin2_metrics_init` | `0x26b660` | `0x278ad0` | `v18_af_latin2_metrics_init` | Latin2 script class `metrics_init` | register-detail difference |
| `af_latin_metrics_init` | `0x26bb4c` | `0x278fbc` | `v18_af_latin_metrics_init` | Latin script class `metrics_init` | register-detail difference |
| `af_latin2_hints_compute_edges` | `0x26c040` | `0x2794b0` | `v18_af_latin2_hints_compute_edges` | Latin2 hint-application path | exact metrics |
| `af_latin_hints_compute_edges` | `0x26c61c` | `0x279a8c` | `v18_af_latin_hints_compute_edges` | Latin hint-application path | exact metrics |
| `af_glyph_hints_align_edge_points` | `0x26cb68` | `0x279fd8` | `v18_af_glyph_hints_align_edge_points` | shared CJK, Latin2, and Latin callers | exact metrics |
| `af_cjk_hints_apply` | `0x26d1f8` | `0x27a668` | `v18_af_cjk_hints_apply` | CJK script class `hints_apply` | exact metrics |

The first two rows supersede the historical v311 labels
`v18_af_latin2_hints_link_segments` and
`v18_af_latin2_hints_compute_edges`. The v311 artifact remains unchanged so
the correction is auditable. The current v312 database uses the CJK names
shown above. All thirteen pairs match normalized ARM64 shape. Eleven match
the complete recorded feature set, and two differ only in register-detail
allocation. All thirteen aliases reopened with zero failures. The v312
database contains 11,695 functions and 392 remaining default `sub_` names.

The machine-readable evidence is
`artifacts/spectron_freetype_autofit_metrics_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_freetype_autofit_metrics_anchors.py`.
The source role references are Android FreeType's
[`afcjk.c`](https://android.googlesource.com/platform/external/freetype/+/a45c6a1cf3625709e149550b8fff1f09d01388d3/src/autofit/afcjk.c),
[`aflatin.c`](https://android.googlesource.com/platform/external/freetype/+/2689da543c08133100124cab3ab19523b04f2f3d/src/autofit/aflatin.c),
[`aflatin2.c`](https://android.googlesource.com/platform/external/freetype/+/6da2e02232e1bcf31cfb78894d46c7902b90ee9/src/autofit/aflatin2.c), and
[`afhints.c`](https://android.googlesource.com/platform/external/freetype/+/8483e21a1fdc252bd234eb55c6b63c17551933ee/src/autofit/afhints.c).
The checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v312.json`.

## Current Spectron FreeType TrueType size-reset label

The v316 pass adds the TrueType driver's size-metrics reset helper. The role is
resolved by the corresponding class-record slot, not by shape alone.

| 1.8 role | Source | Spectron target | Applied label | Callback evidence | Match |
| --- | ---: | ---: | --- | --- | --- |
| `tt_size_reset` | `0x25eaf8` | `0x26bf68` | `v18_tt_size_reset` | TrueType driver class `size_reset` slot at `+0x40` | exact metrics |

The source class record starts at `0x36d3a0` and stores the pointer at
`0x36d3e0`. The target class record starts at `0x380170` and stores the
corresponding pointer at `0x3801b0`. The complete class record retains the
shared `0xd470` displacement. The body is the FreeType TrueType helper that
requests current face metrics, recomputes scaled size metrics, chooses the
active ppem dimension, and marks those metrics valid.

The pair matches all recorded ARM64 feature fields. The alias reopened with
zero failures. The v316 database contains 11,695 functions and 377 remaining
default `sub_` names.

The machine-readable evidence is
`artifacts/spectron_freetype_tt_size_reset_manual_translation_anchor_20260828.json`,
generated by `tools/generate_spectron_freetype_tt_size_reset_anchor.py`. The
source role reference is Android FreeType's
[`ttobjs.c`](https://android.googlesource.com/platform/external/freetype/%2B/6da2e02232e1bcf31cfb78894d46c7902b90ee9f/src/truetype/ttobjs.c).
The checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v316.json`.

## Current Spectron residual target-only labels

The v318 pass assigns explicit target-only names to every residual IDA
function that still had an auto-generated `sub_` name after the v317
cross-build work. These labels describe startup, cleanup, destructor, and
linker roles. They do not claim that stripped Spectron debug symbols or 1.8
source names were recovered.

| Residual role | Count | Label form | Evidence |
| --- | ---: | --- | --- |
| `.fini_array` entries | 117 | `spectron_fini_array_0x...` | target ELF finalizer table range |
| `.init_array` entries | 113 | `spectron_init_array_0x...` | target ELF initializer table range |
| `C8THgaTQxF::clear` wrappers | 99 | `spectron_tstring_clear_wrapper_0x...` | reopened IDA tail-call pseudocode |
| `CanTfaz6bZ::clear` wrappers | 35 | `spectron_can_tfaz6bz_clear_wrapper_0x...` | reopened IDA tail-call pseudocode |
| `vuuHgangcF` destructor thunks | 6 | `spectron_vuu_hgangcf_destructor_thunk_0x...` | reopened IDA destructor target |
| `G0gxgajWBw` destructor thunks | 2 | `spectron_g0gxgajwbw_destructor_thunk_0x...` | reopened IDA destructor target |
| AArch64 PLT resolver | 1 | `spectron_aarch64_plt_resolver` | first target `.plt` slot |
| **Total** | **373** | **No default `sub_` names remain** | **all labels reopened successfully** |

The target-only artifact preserves the original auto-generated name, function
boundary, compact feature fields, and role evidence for each row. This keeps
the distinction visible in tooling: the 3,641 source-backed aliases are
cross-build translations, while these 373 names are stable descriptions of
target compiler and linker artifacts.

The machine-readable record is
`artifacts/spectron_residual_target_only_labels_20260828.json`, generated by
`tools/generate_spectron_residual_target_only_labels.py`. All 373 labels were
applied and reopened with zero failures. The v318 database contains 11,695
functions and no default `sub_` names. The checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v318.json`.

## Current Spectron JPEG and GPC residual labels

The v317 pass translates four residual target functions. The APP14 reader
helper is selected by the marker-reader call sites, the two large GPC helpers
match the scanbeam tree routines, and the small GPC helper is the outlined
allocation diagnostic whose literal says `tristrip node creation`.

| 1.8 role | Source | Spectron target | Applied label | Callback or call-site evidence | Match |
| --- | ---: | ---: | --- | --- | --- |
| `examine_app14` | `0xe0454` | `0xdfae4` | `v18_jpeg_examine_app14` | APP14 branch in marker-reader `save_marker` and `get_interesting_appn` | exact metrics |
| `free_sbtree` | `0x152200` | `0x155028` | `v18_gpc_free_sbtree` | recursive scanbeam-tree release from GPC clipper | exact metrics |
| `build_sbt` | `0x152898` | `0x1556c0` | `v18_gpc_build_sbt` | in-order scanbeam-tree flattening from GPC clipper | exact metrics |
| tristrip node allocation failure path | `0xe01a0` | `0xdf830` | `v18_gpc_tristrip_node_malloc_failure` | GPC `MALLOC` diagnostic called by tristrip construction | register detail |

The two scanbeam functions preserve the shared `0x2e28` displacement in the
GPC region. All four pairs match normalized ARM64 shape. Three match the
complete recorded feature set, while the compiler-extracted diagnostic
differs only in register-detail allocation. The aliases reopened with zero
failures, and the v317 database contains 11,695 functions and 373 remaining
default `sub_` names.

The machine-readable evidence is
`artifacts/spectron_jpeg_gpc_residual_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_jpeg_gpc_residual_anchors.py`. The role
references are libjpeg-turbo's
[`jdmarker.c`](https://github.com/libjpeg-turbo/libjpeg-turbo/blob/main/src/jdmarker.c)
and the General Polygon Clipper
[`gpc.c`](https://github.com/rickbrew/GeneralPolygonClipper/blob/main/gpc.c).
The checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v317.json`.

## Current Spectron libjpeg marker-writer labels

The v315 pass translates the nine residual functions in the bundled libjpeg
marker writer. Seven roles come from the source and target marker-writer
method tables, and the two internal emitters are assigned from their direct
writer callers.

| 1.8 role | Source | Spectron target | Applied label | Callback or call-site evidence | Match |
| --- | ---: | ---: | --- | --- | --- |
| `write_marker_byte` | `0x2986c0` | `0x2a5b30` | `v18_jpeg_write_marker_byte` | marker-writer table `+0x30` | exact metrics |
| `write_file_trailer` | `0x29872c` | `0x2a5b9c` | `v18_jpeg_write_file_trailer` | marker-writer table `+0x18` | exact metrics |
| `write_marker_header` | `0x2987f0` | `0x2a5c60` | `v18_jpeg_write_marker_header` | marker-writer table `+0x28` | exact metrics |
| `emit_dht` | `0x2989a0` | `0x2a5e10` | `v18_jpeg_emit_dht` | scan-header writer caller | exact metrics |
| `write_file_header` | `0x298e90` | `0x2a6300` | `v18_jpeg_write_file_header` | marker-writer table `+0x00` | exact metrics |
| `emit_dqt` | `0x299ac8` | `0x2a6f38` | `v18_jpeg_emit_dqt` | frame and table-only writer callers | exact metrics |
| `write_frame_header` | `0x299e54` | `0x2a72c4` | `v18_jpeg_write_frame_header` | marker-writer table `+0x08` | register detail |
| `write_tables_only` | `0x29a2d8` | `0x2a7748` | `v18_jpeg_write_tables_only` | marker-writer table `+0x20` | register detail |
| `write_scan_header` | `0x29aa48` | `0x2a7eb8` | `v18_jpeg_write_scan_header` | marker-writer table `+0x10` | exact metrics |

All nine pairs match normalized ARM64 shape. Seven match the complete
recorded feature set, while two differ only in register-detail allocation. All
nine aliases reopened with zero failures. The v315 database contains 11,695
functions and 378 remaining default `sub_` names.

The machine-readable evidence is
`artifacts/spectron_jpeg_marker_writer_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_jcmarker_anchors.py`. The source role
reference is libjpeg-turbo's
[`jcmarker.c`](https://github.com/libjpeg-turbo/libjpeg-turbo/blob/main/src/jcmarker.c).
The checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v315.json`.

## Current Spectron FreeType Latin and Latin2 apply labels

The v314 pass translates the remaining large `hints_apply` callbacks in the
contiguous FreeType region. The script class records select the callbacks
directly, so their roles do not depend on the similar shapes of neighboring
helpers.

| 1.8 role | Source | Spectron target | Applied label | Callback or call-site evidence | Match |
| --- | ---: | ---: | --- | --- | --- |
| `af_latin2_hints_apply` | `0x26df5c` | `0x27b3cc` | `v18_af_latin2_hints_apply` | Latin2 script class `hints_apply` | exact metrics |
| `af_latin_hints_apply` | `0x26f820` | `0x27cc90` | `v18_af_latin_hints_apply` | Latin script class `hints_apply` | exact metrics |

Both pairs match the complete recorded ARM64 feature set. Both aliases
reopened with zero failures. The v314 database contains 11,695 functions and
387 remaining default `sub_` names.

The machine-readable evidence is
`artifacts/spectron_freetype_apply_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_freetype_apply_anchors.py`. The source
role references are Android FreeType's
[`aflatin2.c`](https://android.googlesource.com/platform/external/freetype/+/6da2e02232e1bcf31cfb78894d46c7902b90ee9/src/autofit/aflatin2.c) and
[`aflatin.c`](https://android.googlesource.com/platform/external/freetype/+/2689da543c08133100124cab3ab19523b04f2f3d/src/autofit/aflatin.c). The
checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v314.json`.

## Current Spectron bzip2 helper labels

The v313 pass translates the next three residual default names after the
FreeType region. Their source and target addresses share a `0xb470`
displacement, and their callers identify the standard bzip2 allocation hooks
and streaming compression helper.

| 1.8 role | Source | Spectron target | Applied label | Callback or call-site evidence | Match |
| --- | ---: | ---: | --- | --- | --- |
| `default_bzfree` | `0x273350` | `0x2807c0` | `v18_bzip2_default_bzfree` | compression and decompression `bzfree` slot | exact metrics |
| `default_bzalloc` | `0x273360` | `0x2807d0` | `v18_bzip2_default_bzalloc` | compression and decompression `bzalloc` slot | exact metrics |
| `handle_compress` | `0x27336c` | `0x2807dc` | `v18_bzip2_handle_compress` | three `BZ2_bzCompress` action paths | register-detail difference |

The two callbacks are installed when a caller does not provide custom bzip2
stream allocation hooks. `handle_compress` processes input and output state,
flushes run-length data, and calls the block compressor. All three pairs match
normalized ARM64 shape. Two match the complete recorded feature set, and one
differs only in register-detail allocation. All three aliases reopened with
zero failures. The v313 database contains 11,695 functions and 389 remaining
default `sub_` names.

The machine-readable evidence is
`artifacts/spectron_bzip2_helpers_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_bzip2_helpers_anchors.py`. The source
role reference is the bundled bzip2
[`bzlib.c`](https://github.com/libarchive/bzip2/blob/master/bzlib.c). The
checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v313.json`.

## Current Spectron libjpeg compressor preprocessing and downsampling labels

The v295 pass translated ten retained routines from the compressor-side
`jcprepct.c` and `jcsample.c` blocks. The source prep controller is
`jinit_c_prep_controller_jpeg_compress_struct_int` at `0x2aaa44`, and the
target is `v18_jinit_c_prep_controller_jpeg_compress_struct_int` at
`0x2b7eb4`. The source downsampler is
`jinit_downsampler_jpeg_compress_struct` at `0x2abe58`, and the target is
`v18_jinit_downsampler_jpeg_compress_struct` at `0x2b92c8`. The role mapping
follows the callback contracts in the [libjpeg-turbo `jcprepct.c` implementation](https://github.com/libjpeg-turbo/libjpeg-turbo/blob/main/src/jcprepct.c)
and the [libjpeg-turbo `jcsample.c` implementation](https://github.com/libjpeg-turbo/libjpeg-turbo/blob/main/src/jcsample.c).

| 1.8 role | Source | Spectron target | Applied label | Target callback or call site | Match |
| --- | ---: | ---: | --- | --- | --- |
| `start_pass_prep` | `0x2aa4b8` | `0x2b7928` | `v18_jpeg_start_pass_prep` | prep start-pass field at `0x2b7efc` | exact metrics |
| `pre_process_context` | `0x2aa510` | `0x2b7980` | `v18_jpeg_pre_process_context` | context-row branch at `0x2b7fc0` | exact metrics |
| `pre_process_data` | `0x2aa7a4` | `0x2b7c14` | `v18_jpeg_pre_process_data` | simple-buffer branch at `0x2b7f14` | exact metrics |
| `sep_downsample` | `0x2aae40` | `0x2b82b0` | `v18_jpeg_sep_downsample` | public downsample field at `0x2b9310` | exact metrics |
| `int_downsample` | `0x2aaee4` | `0x2b8354` | `v18_jpeg_int_downsample` | integral-ratio branches at `0x2b9334` and `0x2b934c` | exact metrics |
| `h2v1_downsample` | `0x2ab2f4` | `0x2b8764` | `v18_jpeg_h2v1_downsample` | 2:1 horizontal branch at `0x2b948c` and `0x2b9494` | exact metrics |
| `h2v2_downsample` | `0x2ab4a4` | `0x2b8914` | `v18_jpeg_h2v2_downsample` | 2:1 by 2:1 branch at `0x2b9338` and `0x2b9350` | exact metrics |
| `h2v2_smooth_downsample` | `0x2ab670` | `0x2b8ae0` | `v18_jpeg_h2v2_smooth_downsample` | smoothed 2:1 by 2:1 branch at `0x2b933c` and `0x2b9354` | exact metrics |
| `fullsize_smooth_downsample` | `0x2aba28` | `0x2b8e98` | `v18_jpeg_fullsize_smooth_downsample` | smoothed full-size branch at `0x2b93c0` and `0x2b93c8` | exact metrics |
| `fullsize_downsample` | `0x2abcd0` | `0x2b9140` | `v18_jpeg_fullsize_downsample` | full-size branch at `0x2b947c` | exact metrics |

The prep controller's public start callback resets the source-row counter,
conversion buffer, and context state. `pre_process_data` is the simple path:
it color-converts input rows, repeats the bottom row when needed, sends full
buffers to the downsampler, and fills the final iMCU row group. The context
path performs the same work with a circular three-row-group buffer, plus
dummy rows above the first input and below the last input. The target
initializer assigns the two processing callbacks from the `need_context_rows`
flag, which makes this role distinction direct in the stripped binary.

The downsampler initializer first installs `sep_downsample` as the public
dispatcher. It then chooses a one-plane method for each component from its
sampling factors. `int_downsample` handles arbitrary integral ratios;
`h2v1_downsample` and `h2v2_downsample` handle the common 2:1 cases;
`h2v2_smooth_downsample` and `fullsize_smooth_downsample` use context rows
and fixed-point neighbor weights; and `fullsize_downsample` copies samples
and repeats the right edge. The target method-table assignments at
`0x2b92c8` match those source roles directly.

All ten rows are high-confidence matches with identical complete ARM64
feature metrics. Both builds retained default IDA names before this pass, so
the applied names are reviewed semantic roles rather than recovered debug
symbols.

The machine-readable evidence is
`artifacts/spectron_jpeg_preprocessing_downsampling_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_jcprepct_jcsample_anchors.py`. All ten
aliases were applied to an isolated copy of the v294 IDA database and
reopened as the v295 database with zero failures. The broad high-confidence
map still verified all 3,641 earlier aliases with zero failures. The v295
database contains 11,696 functions and 532 remaining default `sub_` names.
Its database SHA-256 is
`a9c756cdd96084cf9dd6fd5ce2c885acd4ea0fffafb6b1c626fafab239c5d284`.
The checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v295.json`.

## Current Spectron libjpeg progressive-Huffman encoder labels

The v294 pass translated eight retained routines from the compressor-side
progressive-Huffman block. The source initializer is
`jinit_phuff_encoder_jpeg_compress_struct` at `0x2aa454`, and the target
initializer is `v18_jinit_phuff_encoder_jpeg_compress_struct` at `0x2b78c4`.
The role mapping follows the progressive encoder callback contract in the
[libjpeg-turbo `jcphuff.c` implementation](https://github.com/libjpeg-turbo/libjpeg-turbo/blob/main/src/jcphuff.c).
Older libjpeg trees may call this compressor file `jphuff.c`; the current
upstream path is `jcphuff.c`.

| 1.8 role | Source | Spectron target | Applied label | Target callback or call site | Match |
| --- | ---: | ---: | --- | --- | --- |
| `start_pass_phuff` | `0x2a73fc` | `0x2b486c` | `v18_jpeg_start_pass_phuff` | public start-pass field at `0x2b78f0` and `0x2b78f8` | normalized shape |
| `emit_eobrun` | `0x2a75dc` | `0x2b4a4c` | `v18_jpeg_emit_eobrun` | shared calls from progressive encoder and finish paths | exact metrics |
| `encode_mcu_AC_refine` | `0x2a7c80` | `0x2b50f0` | `v18_jpeg_encode_mcu_AC_refine` | AC callback branch at `0x2b4994` and `0x2b499c` | normalized shape |
| `finish_pass_phuff` | `0x2a88ec` | `0x2b5d5c` | `v18_jpeg_finish_pass_phuff` | normal finish branch at `0x2b4988` | exact metrics |
| `finish_pass_gather_phuff` | `0x2a8aa4` | `0x2b5f14` | `v18_jpeg_finish_pass_gather_phuff` | gather finish branch at `0x2b48c0` | exact metrics |
| `encode_mcu_DC_refine` | `0x2a8da4` | `0x2b6214` | `v18_jpeg_encode_mcu_DC_refine` | DC refinement branch at `0x2b4978` | exact metrics |
| `encode_mcu_DC_first` | `0x2a922c` | `0x2b669c` | `v18_jpeg_encode_mcu_DC_first` | DC first-scan branch at `0x2b48b0` | exact metrics |
| `encode_mcu_AC_first` | `0x2a9ae0` | `0x2b6f50` | `v18_jpeg_encode_mcu_AC_first` | AC first-scan branch at `0x2b49c4` | normalized shape |

The target start-pass dispatcher checks the progressive spectral-selection and
approximation fields. It selects the DC-first, AC-first, DC-refinement, or
AC-refinement MCU callback, then selects the gather-statistics or normal
finish callback. It prepares the selected DC or AC table and resets EOBRUN,
correction-bit, bit-buffer, and restart state.

`emit_eobrun` is the shared local helper. It turns pending end-of-band runs
into an AC Huffman symbol and payload, drains the correction-bit buffer, and
clears the pending counters. The normal finish callback calls it before
flushing fill bits and saving the destination pointers. The gather finish
callback instead walks the tables used by the scan and generates optimized
Huffman tables from the accumulated counts.

The DC-first callback computes point-transformed DC differences and emits
their Huffman categories and signed magnitude bits. The DC-refinement
callback emits one Al-th bit for each block. The AC-first callback traverses
the natural-order spectral band, emits zero-run and magnitude symbols, and
accumulates EOBRUN for trailing zeroes. The AC-refinement callback separates
newly nonzero coefficients from correction bits, emits their signs and run
lengths, and preserves the correction buffer across the progressive pass.

The target dispatcher assignments identify the four MCU callbacks directly.
The two finish callbacks are selected from `gather_statistics`, and the
shared `emit_eobrun` calls appear in the progressive encoder and normal finish
paths. This is stronger than assuming that the source and target function
order is identical.

All eight rows are high-confidence matches with identical normalized ARM64
feature shape. Five match the complete recorded feature set. The other three
only differ in register allocation detail. Both builds retained default IDA
names before this pass, so these are reviewed semantic roles rather than
recovered debug symbols.

The machine-readable evidence is
`artifacts/spectron_jpeg_progressive_huffman_encoder_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_jcphuff_encoder_anchors.py`. All eight
aliases were applied to an isolated copy of the v293 IDA database and
reopened as the v294 database with zero failures. The broad high-confidence
map still verified all 3,641 earlier aliases with zero failures. The v294
checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v294.json`; it records
11,696 functions and 542 remaining default `sub_` names. Its database
SHA-256 is
`3c01779252a8df94532b47f36c0e2b366d339338db950124f900be6036ae8efd`.

## Current Spectron libjpeg compressor main and master controller labels

The v293 pass translated six retained routines from the compressor-side
`jcmainct.c` and `jcmaster.c` blocks. The source main-controller initializer
is `jinit_c_main_controller_jpeg_compress_struct_int` at `0x2a57bc`, and its
target counterpart is `v18_jinit_c_main_controller_jpeg_compress_struct_int`
at `0x2b2c2c`. The source master-controller initializer is
`jinit_c_master_control_jpeg_compress_struct_int` at `0x2a7058`, and its
target counterpart is `v18_jinit_c_master_control_jpeg_compress_struct_int`
at `0x2b44c8`. The role mapping follows the callback contracts in the
[libjpeg-turbo `jcmainct.c` implementation](https://github.com/libjpeg-turbo/libjpeg-turbo/blob/main/src/jcmainct.c)
and the [libjpeg-turbo `jcmaster.c` implementation](https://github.com/libjpeg-turbo/libjpeg-turbo/blob/main/src/jcmaster.c).

| 1.8 role | Source | Spectron target | Applied label | Target callback or call site | Match |
| --- | ---: | ---: | --- | --- | --- |
| `process_data_simple_main` | `0x2a563c` | `0x2b2aac` | `v18_jpeg_c_process_data_simple_main` | selected by `start_pass_main` at `0x2b2bf4` | exact metrics |
| `start_pass_main` | `0x2a575c` | `0x2b2bcc` | `v18_jpeg_c_start_pass_main` | main-controller start-pass field at `0x2b2c60` | normalized shape |
| `initial_setup` | `0x2a5898` | `0x2b2d08` | `v18_jpeg_initial_setup` | master initialization call at `0x2b4734` | exact metrics |
| `pass_startup` | `0x2a6234` | `0x2b36a4` | `v18_jpeg_pass_startup` | master pass-startup field at `0x2b4510` | exact metrics |
| `finish_pass_master` | `0x2a6270` | `0x2b36e0` | `v18_jpeg_finish_pass_master` | master finish-pass field at `0x2b451c` and `0x2b4524` | exact metrics |
| `prepare_for_pass` | `0x2a6324` | `0x2b3794` | `v18_jpeg_prepare_for_pass` | master prepare-for-pass field at `0x2b4500` | exact metrics |

The main-controller start-pass callback resets the iMCU-row, row-group, and
suspension state, rejects unsupported buffer modes, and selects
`process_data_simple_main`. The row callback sends preprocessed input rows to
the coefficient controller and rolls back the input-row counter when output
suspends. This establishes the two `jcmainct.c` roles through their callback
relationship rather than address order.

The master initializer installs the three public callbacks directly. Its
`initial_setup` call validates component sampling, scan references, table
indexes, and progressive parameters. `prepare_for_pass` selects the active
compression pass and starts the color converter, downsampler, preprocessor,
forward-DCT manager, entropy coder, coefficient controller, and main
controller. `pass_startup` emits deferred frame and scan headers, while
`finish_pass_master` completes entropy output and advances the pass state.

The two `jcmainct.c` rows match normalized ARM64 feature shape; only
`start_pass_main` differs in register allocation detail. The four `jcmaster.c`
rows match the complete recorded feature set. The compressor callbacks use a
`c_` qualifier because the decompressor-side `jdmainct.c` family already has
the unqualified `v18_jpeg_process_data_simple_main` and
`v18_jpeg_start_pass_main` labels at different target addresses.

The machine-readable evidence is
`artifacts/spectron_jpeg_main_master_controller_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_jcmainct_jcmaster_anchors.py`. All six
aliases were applied to a copy of the v292 IDA database and reopened as the
v293 database with zero failures. The v293 checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v293.json`; it records
11,696 functions and 550 remaining default `sub_` names. Its database
SHA-256 is
`98aca07125910b711ab08522d8513d60d75dbf253c627a73f025a7d2a7213590`.
The hash was refreshed after an IDA evidence read persisted packed-database
metadata; the v293 labels and function counts did not change.

## Current Spectron libjpeg compressor Huffman encoder labels

The v292 pass translated five retained routines around the target
`v18_jinit_huff_encoder_jpeg_compress_struct` initializer at `0x2b2a2c`. The
corresponding source initializer is at `0x2a55bc`. The role mapping follows
the compressor Huffman encoder contract in the [libjpeg-turbo `jchuff.c` implementation](https://github.com/libjpeg-turbo/libjpeg-turbo/blob/main/src/jchuff.c).

| 1.8 role | Source | Spectron target | Applied label | Target callback field |
| --- | ---: | ---: | --- | --- |
| `encode_mcu_gather` | `0x2a3cf0` | `0x2b1160` | `v18_jpeg_encode_mcu_gather` | gather-statistics branch at `0x2b2364` and `0x2b236c` |
| `finish_pass_huff` | `0x2a3f30` | `0x2b13a0` | `v18_jpeg_finish_pass_huff` | normal finish branch at `0x2b2510` and `0x2b2518` |
| `encode_mcu_huff` | `0x2a40b0` | `0x2b1520` | `v18_jpeg_encode_mcu_huff` | normal encode branch at `0x2b2514` and `0x2b251c` |
| `start_pass_huff` | `0x2a4ec8` | `0x2b2338` | `v18_jpeg_start_pass_huff` | public start-pass field at `0x2b2a58` and `0x2b2a60` |
| `finish_pass_gather` | `0x2a54a4` | `0x2b2914` | `v18_jpeg_finish_pass_gather` | gather finish branch at `0x2b2360` and `0x2b2368` |

The start-pass dispatcher selects the gather-statistics pair when optimized
Huffman tables are being collected and the normal encode and finish pair for
ordinary output. It also prepares the derived or statistics tables for every
component, resets DC predictors, and initializes the bit-buffer and restart
state. The gather encoder counts DC categories and AC run-length symbols
without writing output. The normal encoder emits restart markers, Huffman
codes, stuffed bytes, and suspended destination state. The two finish
callbacks either flush the pending bit buffer or generate optimized tables
from the accumulated counts.

The target `v18_jinit_huff_encoder_jpeg_compress_struct` initializer stores
`v18_jpeg_start_pass_huff` in the public callback field. The target
start-pass body then selects `v18_jpeg_encode_mcu_gather` and
`v18_jpeg_finish_pass_gather` for the statistics pass, or
`v18_jpeg_encode_mcu_huff` and `v18_jpeg_finish_pass_huff` for normal output.
These callback assignments identify all five routines independently of
address order.

All five rows are high-confidence matches with identical normalized ARM64
feature shape. Two match the complete recorded feature set. The other three
differ only in register allocation detail, with the start-pass row also
showing the expected target export names for shared helper calls. Both builds
retained default IDA names before this pass, so these are reviewed semantic
roles rather than recovered debug symbols.

The machine-readable evidence is
`artifacts/spectron_jpeg_huffman_encoder_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_jchuff_anchors.py`. All five aliases
were applied to a copy of the v291 IDA database and reopened as the v292
database with zero failures. The v292 checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v292.json`; it records
11,696 functions and 556 remaining default `sub_` names. Its database
SHA-256 is
`4ad75f4e344be7e0d168d3674b0bd93b743d5148448ec7a2b50f559fb48f7c09`.

## Current Spectron libjpeg forward-DCT manager labels

The v291 pass translated three retained routines around the target
`v18_jinit_forward_dct_jpeg_compress_struct` initializer at `0x2b1070`. The
corresponding source initializer is at `0x2a3c00`. The role mapping follows
the forward-DCT manager contract in the [libjpeg-turbo `jcdctmgr.c` implementation](https://github.com/libjpeg-turbo/libjpeg-turbo/blob/main/src/jcdctmgr.c).

| 1.8 role | Source | Spectron target | Applied label | Target callback field |
| --- | ---: | ---: | --- | --- |
| `start_pass_fdctmgr` | `0x2a31f0` | `0x2b0660` | `v18_jpeg_start_pass_fdctmgr` | public start-pass field at `0x2b10a0` |
| `forward_DCT` | `0x2a3638` | `0x2b0aa8` | `v18_jpeg_forward_DCT` | integer methods at `0x2b1128` and `0x2b1144` |
| `forward_DCT_float` | `0x2a37a0` | `0x2b0c10` | `v18_jpeg_forward_DCT_float` | floating-point method at `0x2b110c` |

The start-pass routine walks the component quantization tables and prepares
the multiplier storage used by the selected integer or floating-point DCT
path. The integer routine centers each 8 by 8 sample block, calls the
selected integer DCT implementation, applies the quantization divisors, and
writes 64 signed coefficients. The floating-point routine performs the same
work through the float DCT callback and floating quantization multipliers.

The target initializer provides direct callback evidence. It stores
`v18_jpeg_start_pass_fdctmgr` in the public start-pass field, selects
`v18_jpeg_forward_DCT` for both accurate and fast integer methods, and selects
`v18_jpeg_forward_DCT_float` for the floating-point method. This is stronger
than an address-order match because each residual body is tied to a distinct
branch in the initializer.

All three rows are high-confidence matches with identical normalized ARM64
feature shape. Two match the complete recorded feature set. The only
exception is register allocation detail in `start_pass_fdctmgr`, which is
recorded in the machine-readable artifact. Both builds retained default IDA
names before this pass, so these are reviewed semantic roles rather than
recovered debug symbols.

The machine-readable evidence is
`artifacts/spectron_jpeg_forward_dct_manager_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_jcdctmgr_anchors.py`. All three aliases
were applied to a copy of the v290 IDA database and reopened as the v291
database with zero failures. The v291 checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v291.json`; it records
11,696 functions and 561 remaining default `sub_` names. Its database
SHA-256 is
`9933c9b80f7962b4e3666ad5f6eee22b42487e8027d2691fb9e36abd6d1e76a4`.

## Current Spectron libjpeg baseline-Huffman decoder labels

The v287 pass translated the two retained callback routines immediately
before the target `v18_jinit_huff_decoder_jpeg_decompress_struct` initializer
at `0x2ad09c`. The corresponding source initializer is at `0x29fc2c`. The
role names follow the callback contract in the [libjpeg-turbo `jdhuff.c` implementation](https://github.com/libjpeg-turbo/libjpeg-turbo/blob/main/src/jdhuff.c).

| 1.8 role | Source | Spectron target | Applied label | Target use | Match |
| --- | ---: | ---: | --- | --- | --- |
| `start_pass_huff_decoder` | `0x29f2d0` | `0x2ac740` | `v18_jpeg_start_pass_huff_decoder` | entropy start-pass slot at `0x2ad0c4` | exact metrics |
| `decode_mcu` | `0x29f734` | `0x2acba4` | `v18_jpeg_decode_mcu` | baseline MCU decoder slots at `0x2ad0d4` and `0x2ad0dc` | reviewed relocation exception |

The start-pass routine checks the decompression state, builds the derived DC
and AC Huffman tables for each component, initializes the per-component
progression fields, and resets the bit reader and restart state. The target
initializer stores it in the first entropy callback field.

The MCU decoder reads one baseline JPEG MCU from the bitstream. It decodes
the DC difference and updates the component predictor, then expands AC
run-length symbols, handles end of block and zero runs, writes coefficients in
natural order, and saves the bit-reader state when input suspends. The target
initializer stores the same routine in both baseline decoder callback slots.

Both rows are high-confidence matches. The start-pass row matches all
recorded feature metrics. The MCU decoder has the same size, instruction
count, basic-block count, branch and call counts, return count, and mnemonic
sequence as the source. Its operand-shape hashes differ because one `ADD`
instruction references a relocated `PAGEOFF` constant whose low-page bucket
is different in the two builds. That difference is recorded explicitly in
`artifacts/spectron_jpeg_baseline_huffman_manual_translation_anchors_20260828.json`;
it is not being presented as a full metric match.

The machine-readable evidence is generated by
`tools/generate_spectron_jdhuff_anchors.py`. Both aliases were applied to a
copy of the v286 IDA database and reopened as the v287 database with zero
failures. The v287 checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v287.json`; it records
11,696 functions and 579 remaining default `sub_` names. Its database
SHA-256 is
`5af0d676931b984f0eac35dfa49ff13f66b11f5b3a8c023f19a9d03fc26b844e`.

## Current Spectron libjpeg inverse-DCT manager label

The v286 pass translated the retained start-pass routine immediately before
the target `v18_jinit_inverse_dct_jpeg_decompress_struct` initializer at
`0x2abc20`. The corresponding source initializer is at `0x29e7b0`. The role
follows the inverse-DCT manager contract in the [libjpeg inverse-DCT manager source](https://github.com/libjpeg-turbo/libjpeg-turbo/blob/main/src/jddctmgr.c).

| 1.8 role | Source | Spectron target | Applied label | Target use | Match |
| --- | ---: | ---: | --- | --- | --- |
| `start_pass` | `0x29e40c` | `0x2ab87c` | `v18_jpeg_idct_start_pass` | public IDCT start-pass slot at `0x2abc5c` | normalized shape |

The callback selects the 1 by 1, 2 by 2, 4 by 4, accurate integer, fast
integer, or floating-point inverse-DCT method for each component. When the
method changes, it rebuilds the associated multiplier table from the active
quantization table. The target body preserves those cases, per-component
method state, and table-generation paths, while its initializer stores the
callback in the same public slot as the source.

The source and target rows are a high-confidence match. All normalized ARM64
feature fields match, and the only difference is register allocation detail.
The target's existing IDCT implementation names, including the scaled and
floating-point variants, make the method-selection switch independently
checkable in the decompiled body.

The machine-readable evidence is
`artifacts/spectron_jpeg_inverse_dct_manager_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_jddctmgr_anchors.py`. The alias was
applied to a copy of the v285 IDA database and reopened as the v286 database
with zero failures. The v286 checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v286.json`; it records
11,696 functions and 581 remaining default `sub_` names. Its database
SHA-256 is
`87624a85eae15f9520bfcbf1b356121aa7af6b97db66997a66bcea5baadadae9`.

## Current Spectron zlib inflate_fast residual label

The v272 pass resolved one large residual helper by combining call-site
identity, body semantics, strings, and the complete recorded feature set. The
source and target functions both retained default names, so this is a
semantic library-role label rather than a restored debug symbol.

| zlib role | 1.8 current name | Source | Spectron current name | Target | Applied label | Match |
| --- | --- | ---: | --- | ---: | --- | --- |
| `inflate_fast` | `sub_28A2F4` | `0x28a2f4` | `sub_297764` | `0x297764` | `v18_zlib_inflate_fast` | all metrics except register detail |

The source helper is called by `inflate` at `0x28566c`, and the target helper
is called by `v18_inflate` at `0x292adc`. Both bodies contain the zlib
`invalid literal/length code`, `invalid distance code`, and `invalid distance
too far back` strings. Their main path decodes literal/length and distance
Huffman entries, then copies the matched bytes into the output window. The
source and target ranges are both 2,724 bytes with 135 basic blocks and 678
instructions. All normalized fields and the mnemonic hash match; only the
register-allocation detail hash differs.

The machine-readable evidence is
`artifacts/spectron_zlib_inflate_fast_manual_translation_anchor_20260828.json`,
generated by `tools/generate_spectron_zlib_inflate_fast_anchor.py`. The label
was applied to the v272 database and reopened with zero failures.

The same residual search produced a useful rejection. A shape-only collision
suggested the source `jpeg_open_backing_store` name for target `0x2a25ac`.
Target pseudocode instead writes the value `46` into a JPEG object field and
dispatches through the object callback; its callers are inside
`v18_jinit_1pass_quantizer_jpeg_decompress_struct`. That evidence does not
support the suggested name, so the target function remains a default name.

## Current Spectron TGraalVar script-runtime labels

The v269 pass reaches the small TGraalVar script callback block that remained
unnamed after the broader function translation. These rows are stronger than
address proximity alone because the source and target callback cells can be
checked directly in the corresponding tables.

| 1.8 callback | Source | Spectron target | Applied label | Source callback cell | Target callback cell | Match |
| --- | ---: | ---: | --- | ---: | ---: | --- |
| `TGraalVar_script_clearvars` | `0x20d26c` | `0x21362c` | `v18_TGraalVar_script_clearvars` | `0x3875c8` | `0x39a718` | exact metrics |
| `TGraalVar_script_savejsontostring` | `0x20d4dc` | `0x2137ec` | `v18_TGraalVar_script_savejsontostring` | `0x387b98` | `0x39ace8` | exact metrics |
| `TGraalVar_script_parsejson` | `0x20d500` | `0x213810` | `v18_TGraalVar_script_parsejson` | `0x387d18` | `0x39ae68` | exact metrics |
| `TGraalVar_script_loadini` | `0x20d50c` | `0x21381c` | `v18_TGraalVar_script_loadini` | `0x3879b8` | `0x39ab08` | exact metrics |
| `TGraalVar_script_addnamedstring` | `0x20d3e8` | `0x2138b0` | `v18_TGraalVar_script_addnamedstring` | `0x387508` | `0x39a628` | layout change |

The four exact rows are short forwarding wrappers. `clearvars` dispatches to
virtual slot +56. `savejsontostring` calls the target JSON writer,
`parsejson` calls the target JSON reader when the result is non-null, and
`loadini` forwards the constant enabled flag `1` to the target INI helper.
The `addnamedstring` body keeps the source variable lookup and assignment to
result offset +8, but the target adds a named-string temporary and uses the
target `CanTfaz6bZ` wrapper. Its table adjacency and decoded `addnamedstring`
record make the semantic match high confidence even though its size grows
from 72 to 120 bytes.

One nearby callback is target-specific. The target row at `0x39aa90` decodes
to `loadvarsfromarray` and points through callback cell `0x39aaa8` to
`0x218870`, now labeled
`spectron_TGraalVar_script_loadvarsfromarray_TGraalVar`. Its body creates a
string list, reads each non-null array member through the target virtual
`readString` slot, and passes the list to the adjacent
`v18_TGraalVar_loadVarsFromArray_TStringList` implementation. No matching 1.8
script callback is claimed for this target-only label.

The machine-readable records are
`artifacts/spectron_tgraalvar_script_runtime_manual_translation_anchors_20260828.json`
and
`artifacts/spectron_tgraalvar_target_only_labels_20260828.json`. The v269 IDA
copy reopened with all five cross-build names and the target-only name
verified, with zero failures.

## Current Spectron Android security labels

The v265 copy extends the cross-build work into the legacy Android and TapJoy
callbacks. The target table and the reviewed target bodies connect three
small callbacks and one larger JNI helper to their 1.8 roles. The target
build uses different Android and string wrappers, so none of these rows is an
instruction-metric exact match. The evidence is still strong because the
credential slots, Java method signature, table context, and Android ID access
path agree.

| 1.8 role | Source | Spectron target | Applied label | Target table context |
| --- | ---: | ---: | --- | ---: |
| `MainAndroid_script_settapjoysecret` | `0x2401f4` | `0x24a240` | `v18_MainAndroid_script_settapjoysecret` | `0x39e788` |
| `MainAndroid_script_settapjoyapplicationid` | `0x240204` | `0x24a254` | `v18_MainAndroid_script_settapjoyapplicationid` | `0x39e758` |
| `JNI_connectToTapJoyService` | `0x2410ac` | `0x24c7e4` | `v18_JNI_connectToTapJoyService` | `0x39e7b8` |
| `androidGetID_void` | `0x2435e8` | `0x2502f4` | `v18_androidGetID_void` | not registered in either table |

The two credential setters write the first and second cached strings that the
connector later converts to Java byte arrays. The connector resolves
`connectToTapJoyService([B[B)Z`, calls it, releases both arrays, and returns
the Java boolean. The Android ID helper walks from the ActivityThread to the
application content resolver and reads `Settings.Secure.ANDROID_ID`. The
machine-readable evidence is
`artifacts/spectron_android_legacy_manual_translation_anchors_20260828.json`,
generated by `tools/generate_spectron_android_legacy_anchors.py`. The v265
database has 11,696 functions and 709 default `sub_` names. Its SHA-256 is
`89eafac9cb7b6cba867fa6e39c0fd6e6814a0b45b586e5d75238b2502e566e61`, and the
checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v265.json`.

The v266 copy initially added five target-only labels. A table-indexing error
in that first artifact assigned the `getjavaclassexists` description to
`0x2500ec`, even though the raw target row at that callback is
`getstaticjavafuncexists`. The v267 correction rechecked the adjacent rows,
renamed that function, and added the missed `getjavaclassexists` callback at
`0x250090`. These names are intentionally separate from the 1.8
correspondences. The corrected set contains six labels. Five come from the
target Android helper table and one describes the nonreturning nanosleep loop
called by the retained Frida detection routines.

| Target address | Applied label | Evidence role |
| ---: | --- | --- |
| `0x24a1d8` | `spectron_getandroidabi` | target table callback `getandroidabi` |
| `0x250090` | `spectron_android_getjavaclassexists` | target table callback `getjavaclassexists` |
| `0x2500ec` | `spectron_android_getstaticjavafuncexists` | target table callback `getstaticjavafuncexists` |
| `0x2501f0` | `spectron_android_getjavafuncexists` | target table callback `getjavafuncexists` |
| `0x24a188` | `spectron_setactiononfridadetected` | target table callback and Frida action global |
| `0x24a2ac` | `spectron_frida_detection_sleep_loop` | nanosleep loop called by `DetectFridaLoop1` |

The existing retained names at `0x24a1ac` and `0x24a2e8` remain
`DetectFridaLoop2` and `DetectFridaLoop1`. The new loop label records its
observed control flow, but does not prove that anti-instrumentation is the
reason the client fails to start. The corrected target-only evidence is in
`artifacts/spectron_android_security_target_only_labels_corrected_20260828.json`,
generated by `tools/generate_spectron_android_security_labels.py`. The v267
database has 11,696 functions and 703 default `sub_` names. Its SHA-256 is
`a2c29b06f0fed5b7631586051e28b246a28882c057bfe808ff7db32516a20a5f`, and the
checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v267.json`.

The earlier v266 artifact remains in the archive as a historical record of
the mistaken table pairing. The corrected artifact explicitly records the
repair and accepts the old v266 name at `0x2500ec` as a previous label during
the IDA rename pass.

The v268 copy resolves the remaining package-signature helper. The target
script table row at `0x39f0f0` names the callback
`quattro::android::getsignature` and points through callback cell
`0x39f108` to `0x24a9ec`. The body obtains the application package name,
calls `PackageManager.getPackageInfo(packageName, 0x40)`, reads
`signatures[0]`, and calls `toCharsString()`. It returns the script string
`null` when the signature object is absent. The applied target-only label is
`spectron_quattro_android_getsignature`. The machine-readable evidence is
`artifacts/spectron_android_package_identity_labels_20260828.json`, generated
by `tools/generate_spectron_android_package_identity_labels.py`. The v268
database has 11,696 functions and 702 default `sub_` names. Its SHA-256 is
`00d08f743e7e01ac77b6eb8ccec266db89be0c8cc2382ebd542e23f2d80a4077`, and the
checkpoint is
`artifacts/spectron_translation_checkpoint_20260828_v268.json`.

## Naming policy

The native names are kept close to their demangled ELF form. Characters that
are inconvenient in an IDA identifier are converted to underscores, while
the original mangled symbol remains in the export. PLT entries receive a
`plt_` prefix and jump thunks receive a `j_` prefix so that a thunk is not
mistaken for the implementation it reaches.

Examples:

| Native role | Applied alias | Address in ARM64 database |
| --- | --- | ---: |
| Connector mode selection | `TServerList_enterNextConnectorMode_int` | `0x203df4` |
| Connector login | `TServerList_login` | `0x204420` |
| Game-server connect | `TClient_connectToGameServer` | `0x1e7058` |
| HTTP request creation | `THTTPRequest_sendRequest` | `0x1ffde8` |
| Incoming packet dispatch | `TClient_parse` | `0x1e7cd0` |
| Encrypted level loader | `TServerLevel_LoadEncrypted_void` | `0x1aa198` |

The exact address and alias for every symbol are in the CSV. These names are
analysis labels, not an assertion that every demangled signature was manually
verified. The important connector and packet functions were checked against
cross-references and emulator traces.

Some important callbacks have no surviving ELF symbol. The IDA database now
labels the game-server SSL method at `0x1eb964` as
`TClient_setSSLParameters_scriptCallback`. The `scriptCallback` suffix marks
it as an inferred semantic name, not part of the original symbol import. Its
method-table reference and native behavior are documented in
`artifacts/game_server_tls.json`.

The same database now has evidence-backed names for the login callback,
server-list completion, file-download, and script-window helpers that IDA
originally displayed as `sub_` functions. Examples include
`TClient_handleServerLoginPacket`, `TClient_finishFileDownload`, and
`TGUIScriptLoader_finishServerListConnect`. These remain separate from the
imported symbol CSV because they are semantic labels, not ELF names. The
complete list and the evidence behind each label are in
`artifacts/ida_semantic_labels.json`.

The latest pass extends that coverage across the whole core `TClient` protocol
region, from `0x1e9000` through `0x1f3000`. It labels map entry, NPC creation
and removal, object and effect updates, encrypted-script dispatch, ping and
text handling, and the inbound and outbound handler bridges. A few wrappers
use exact offset-based names such as `TClient_setPlayerStateFlag2328` or
`TClient_getConnectionString8288`. That is deliberate. The instructions prove
the field offset or virtual slot, but they do not recover the original C++
member name. Keeping the uncertainty visible makes these aliases safer to use
in later patches. The current pass also covers the `TGraalConnection` and
`THTTPRequest` TLS or HTTP property bridge, `TSocket` policy and plain-I/O
helpers, and `TGaniObject` or `TGaniParam` field accessors. Those names use the
same offset-based convention where the binary proves a field but not its source
member name. It also covers the network-thread entry point and the
`TUpdatePackage` metadata, download-progress, and script-update wrappers. The
update-package labels use exact field offsets for the remaining undocumented
members.

## Complete pre-persistence function inventory

The symbol alias export and the IDA function list are different sets. The
alias export contains 8,601 rows, including 505 data aliases. Of those rows,
8,096 land on IDA functions. The APK itself contributes 6,506 defined dynamic
symbol rows, while the alias export keeps PLT and jump-thunk views separate and
includes analysis-visible data names. IDA's first analysis snapshot adds
11,272 function starts in total:

| Function source | Count |
| --- | ---: |
| Backed by a translated alias row | 8,096 |
| IDA default `sub_` names | 1,645 |
| Named by IDA but not backed by an ELF record | 1,531 |
| Total IDA functions | 11,272 |

The complete address-level inventory for that first snapshot is in
`symbols/libqplay.function_inventory.csv` and
`symbols/libqplay.function_inventory.json`. Every row records the IDA name,
address, segment, size, incoming-reference count, thunk and library flags,
and the matching original ELF symbol when one exists. The summary file records
the input hash and counts. This is the honest limit of the available evidence:
the remaining default `sub_` entries are real functions identified by IDA, but
the APK does not contain source names for them. They remain addressable and
searchable without being given guesses that could mislead later protocol work.
The semantic pass names only the small set whose behavior was clear enough to
document. The JNI window is now labeled by the Java method name where the
native call site contains an exact method string. This covers social, store,
URL, keyboard, and device-information bridges, plus
`JNI_setVideoPlayerRectangle` at `0x242df0`. Two string-cache setters at
`0x2401f4` and `0x240204` remain address-based `sub_` entries because their
caller contract is not yet recovered.

The later persisted copies are documented separately in
`docs/IDA_RESIDUALS.md`. The base copy contains 11,297 function starts after
the `.eh_frame` callback boundaries were created and leaves 459 default names.
A follow-up CyaSSL role pass produced a new 11,297-function copy with 448
default names. The static-library role pass then produced a v4 copy with 421
default names. The base saved-copy hash and close-and-reopen verification are
in `artifacts/ida_translation_validation.json`; the role audits and residual
list are in `artifacts/cyassl_static_role_audit_20260826.json`,
`artifacts/static_library_role_audit_20260826.json`, and
`artifacts/ida_residual_profile.json`.

To make the handoff from the user's desktop session explicit, I also copied
the exact active snapshot before applying the same pass. Its source database
had SHA-256
`56da88101fe904ca298dcadf31e90433a69c43818c681ccb72364c66ac99eaa4`.
The translated local copy is
`/home/v/Desktop/graal-decomp/analysis/libqplay_translated_from_active.i64`.
A clean IDA 9.3 save and a separate reopen verified all 1,211 names, 11,297
function starts, and 459 remaining default names. The copy is intentionally
local and ignored by the public repository. Its SHA-256 after the final
reopen was
`1b72e100a755939c973ff1c4dcba2c8a9915a454f5f89c4c5d781bd74add421e`.
IDA may update database metadata when that file is opened again, so the hash
is an audit checkpoint rather than a content-addressed release identifier.

The sound script table is also partly recovered. The wrappers at
`0xe1e0c`, `0xe22e8`, `0xe24c4`, `0xe2858`, and `0xe2a7c` now carry
script-prefixed aliases for `play2`, `playlooped2`, `playlooped`,
`setsoundpitchbynote`, and `setsoundpitch`. The adjacent `0xe2008` wrapper is
labeled `TSounds_script_play` from its position in the play overload pair and
its action-NPC-centered behavior. The table names are encoded with the native
`THashList::encodesimple` transform, which is recorded in the research notes.

The environment tables now also expose the remaining script wrappers in this
region: `md5`, `adventure_quit`, the shared `googleplay` version helper, and
the OS, network, and system identification calls. These aliases preserve the
script-facing names with a `script` component so they are not confused with
the underlying C++ methods.

The input and level-object property tables provide another set of exact names.
The input table identifies the hardware-keyboard getter and setter. The control
binding table identifies the `action`, `keycode`, `keytext`, and `slot` getters.
The level-object table identifies the `level`, `x`, `y`, `z`, and `layer`
properties, including their coordinate clamping and vtable forwarding behavior.
The `z` getter was initially present as a code pointer without an IDA function
boundary, so the boundary at `0x169a08` was defined from the property table
reference before applying its label.

The `GuiControlProperties` constructor at `0x1b45c8` registers 55 properties
from the table at `0x3806a0`. That table accounts for the bounds, extent,
client size, clipping, profile, color, visibility, animation, sizing, and
position accessors now labeled in the IDA database. The companion function
table at `0x3810f0` registers 28 script functions. Eleven of its formerly
unnamed wrappers are now labeled, including coordinate conversion, control
lookup, resize, repaint, show or hide, and first-responder helpers. The
`minextent` and `minsize` properties intentionally share one getter and setter
pair, and the table's encoded `showhint` terminator is recorded as an encoding
artifact rather than treated as a different property name.

The file-scripting table at `0x376bd0`, installed by
`TFileScripting_initStaticScriptVars` at `0xfd1d0`, contains 27 script
functions. Twenty-two formerly unnamed callbacks now have exact
script-prefixed labels for script-access paths, file existence and size,
filename escaping, timestamps, folder enumeration, file content, default
viewers, deletion, and resource decompression. The wrappers distinguish
ordinary filesystem paths from packaged level resources, so this table is
useful when tracing startup resource lookup and updates.

A fallback disassembly pass also recovered the seven callback roles installed
by `TStream_fillZipFunctions` at `0xf0e98`: open, read, write, tell, seek,
close, and error. It also identified several compiler-generated static-state
helpers for `TFiles`, `TClient`, `TSocket`, `TServerFlying`, resource link
lists, the restart state, the `TDrawTexture::textures` list, `curanis`,
`TOptions::windowpos`, and `displayedgif`. The ELF relocation records tie the
latter four routines to their global objects. The adjacent sound wrappers also
cover `TSounds::isMusicPlaying` and the exported
`TSounds::soundoffscreendistance` getter and setter. A follow-up read of the
same registration block identified `TSounds_getMusicFilename` and the getter
and setter for `TSounds::disabledsoundeffects`. The proposed names are still
kept in
`artifacts/native_callback_candidates.json` as a review artifact, not counted
among the 467 imported semantic labels. The native callback and script-table
passes were later written to the persisted disposable copy and verified there
with a fresh inventory export. The active desktop database was not changed.
The candidate plan at this stage contained 25 entries.
The same sound table also contributes the short `stopsounds` and
`setmusicvolume` wrappers, bringing the candidate plan to 27 entries.

The server-level property and function tables were the next group in the
review queue.
`TServerLevelProperties::TServerLevelProperties` at `0x1a1128` registers six
properties from `0x37fce0`, and eighteen script functions from `0x37fe00`.
The property names decode to `height`, `isnopkzone`, `issparringzone`,
`nopkzone`, `tilelayercount`, and `width`. The function names decode to
`getmappartfile`, `findareanpcs`, `putbomb`, `putbomb2`, `putexplosion`,
`putexplosion2`, `reflectarrow`, `removearrow`, `removebomb`, `removeexplo`,
`removeitem`, `shoot`, `testbomb`, `testexplo`, `testitem`, `testsign`,
`testnpc`, and `tiletype`.

The callback bodies provide an unusually complete cross-check. The property
accessors read layer dimensions, zone flags, and the layer-list count. The
function wrappers call the exported `TServerLevel` methods for bombs,
explosions, projectiles, tiles, and collision tests, or delete entries from
the corresponding object lists. The raw `reflectarrow` record contains the
known encoded-zero sentinel, which `THashList::codesimplefix0` now repairs.
The short names `removeexplo` and `testexplo` are preserved exactly as stored
by the client.

The twenty-four proposed names are kept separate in
`artifacts/native_callback_candidates.json`. They brought the review native
candidate set to 51 entries. The current active IDA inventory and the 467
imported semantic labels remained unchanged until the bridge could accept and
verify the batch.

The player table is the next large group. Its constructor at `0x18b9bc`
registers 52 properties from `0x37ce00` and six script functions from
`0x37d7c0`. The names cover player identity, chat, inventory, image state,
coordinates, channel state, ratings, and external-message helpers. Four sets
of properties intentionally share native targets: `fullhearts` with `maxhp`,
`gralats` with `rupees`, `head` with `headimg`, and `hearts` with `hp`.
The candidate artifact records one name per native target and explains each
alias. The nickname getter is included, while the setter already has an ELF
name through the `TServerPlayer::setNick` jump.

The six player callbacks decode to `isguildpm`, `ismasspm`, `pmswaiting`,
`openexternalhistory`, `openexternalpm`, and `showprofile`. The pointer at
`0x18aa68` was present in the table but lacked an IDA function boundary in the
saved inventory, so the application helper reported it for manual boundary
recovery. The 74 new candidates brought the review set to 125.

The NPC constructor at `0x183c18` adds 26 properties from `0x37be28` and 57
script callbacks from `0x37c308`. Their names cover the NPC's health, image,
layer, collision, pelt, weapon, visibility, movement, carry, drawing,
projectile, and show or hide behavior. The property table shares its hearts
and HP accessors, and its image and sprite targets include two inherited ELF
jumps that are already named. Four callback pointers, at `0x180e50`,
`0x18402c`, `0x181d58`, and `0x1a4e98`, needed IDA function boundaries. The
remaining 37 property targets and all 57 script targets are recorded in
`artifacts/native_callback_candidates.json` under the two NPC groups.

Several NPC records, including `peltwithbush`, `peltwithsign`, `peltwithvase`,
`canbecarried`, and `showtext`, contain the old encoded-zero sentinel. The
decoder repairs those bytes, while table position and callback context provide
additional cross-checks for the spellings.
The 94 new NPC candidates brought the review set to 219.

The smaller server-object constructors provide another compact set of exact
table-backed names. `TServerWeaponProperties` at `0x190ca4` registers
`isweapon` from `0x37d8e0`. The bomb, explosion, chest, extra, flying-object,
and sign constructors register their tables at `0x38b058`, `0x38afc8`,
`0x38b0e8`, `0x38b148`, `0x38b1a8`, and `0x38b298`. Those records recover
`power`, `time`, `image`, `dir`, `isopen`, `item`, `type`, `dx`, `dy`, `from`,
and `text`, with the class prefix retained in each proposed native name.

`TServerCarryProperties` at `0x23d694` and `TServerLeapProperties` at
`0x23fde8` do not call `TScriptProperty::addProps` and are recorded as
metadata-only constructors. The seven new candidate groups add 23 unique
native targets, raising the review set from 219 to 242. They were later
applied and verified in the persisted disposable copy; the applier remains
review-only for other IDA databases.

The projectile table at `0x37f6d8` contributes ten read-only names, including
`x`, `y`, `z`, `angle`, `speed`, `zspeed`, `fromplayer`, `fromplayerid`, and
`params`. The level-link table at `0x37f9b0` contributes seven read-only names:
`destlevel`, `destx`, `desty`, `height`, `width`, `x`, and `y`. Their
constructors are `TProjectileProperties` at `0x19ecac` and
`TServerLevelLinkProperties` at `0x1a0494`.

`TTilesLayerProperties` at `0x1a0df4` registers nine properties from
`0x37fb00` and one function from `0x37fcb0`. The function name is
`updateboard`; eight properties have setters and `layerindex` is read-only.
These three groups add 35 unique targets, raising the review set to 277. They
were later applied and verified in the persisted disposable copy; the applier
remains review-only for other IDA databases.

The complete registration scan is in `artifacts/script_table_inventory.json`.
The scan finds 70 property tables and 62 function tables through direct calls to the
two imported `TScriptProperty` registration stubs. The tables contain 678
property records and 776 static function records, plus one dynamic Android
registration slot. The records resolve to 1,779 unique callback targets.

The inventory distinguishes 411 names already present in the semantic-label
artifact, 258 in the curated callback candidate artifact, 204 existing
non-default IDA names, 886 exact new names with saved function boundaries, and
20 exact pointers without saved boundaries. The decoder models the old
zero-byte encoding behavior, so all 1,454 static record names are recovered
exactly. The remaining declared slot is the dynamic Android registration path.
The exact bounded set has a review-only IDA applier in
`tools/ida_apply_script_table_inventory.py`; it is not enabled while the IDA
bridge is unavailable. Each missing boundary has a matching ELF `.eh_frame`
range, and `tools/ida_apply_script_table_boundaries.py` provides a separate
review-only boundary and rename pass for those 20 callbacks.

The four apply passes were then run together in IDA 9.3 IDALIB against a
disposable copy of the database. All 277 native callback candidates, 886
bounded script-table names, 20 FDE-backed script callbacks, and 28 unresolved
application or engine roles resolved at the expected addresses with zero
failures. Five native callback ranges and all twenty script-table ranges were
created from ELF unwind evidence. Two of the latter required shortening a
larger saved IDA function at the exact FDE start before adding the callback.
This first established the names and ranges in memory. A follow-up IDALIB
run saved the disposable database as a packed IDA 9.3 copy, and a separate
read-only process reopened it and found every one of the 1,211 expected names
at the expected address. The base copy contains 11,297 function starts and
459 remaining default `sub_` names. The active desktop IDA database was still
locked and was not changed. The result and saved-copy hash are recorded in
`artifacts/ida_translation_validation.json`.

The separate CyaSSL pass then renamed eleven static certificate, crypto, and
TLS routines in a new disposable copy. A clean reopen verified all eleven
aliases at their expected addresses, kept the total at 11,297 functions, and
left 448 default names. Seven names match recognizable historical CyaSSL
roles, while four are descriptive aliases whose exact source names are not
preserved. The pass is recorded in
`artifacts/cyassl_static_role_audit_20260826.json`, and the reusable IDA
scripts are `tools/ida_apply_cyassl_static_aliases.py` and
`tools/ida_verify_cyassl_static_aliases.py`.

The static-library pass then renamed 27 more functions in a v4 disposable
copy. It covers 14 zlib routines, four bzip2 sorting and MTF routines, two
minizip ZIP-header helpers, one GPC edge-table builder, two CyaSSL ASN helpers,
the shared LibTomCrypt DES transform, and three YAJL allocator callbacks. A
clean reopen verified all 27 names and reduced the default count from 448 to
421. The complete source-role evidence and v4 hash are in
`artifacts/static_library_role_audit_20260826.json`; the combined residual
list is in `artifacts/ida_residual_profile.json`.

Exporting the base saved copy also gives a useful coverage snapshot: 8,096
functions are backed by ELF records, 2,742 have non-default IDA names from
the semantic and callback passes, and 459 remain default `sub_` entries. The
The v3 CyaSSL copy has 2,753 named non-ELF functions and 448 default entries.
The v4 static-library copy has 2,780 named non-ELF functions and 421 default
entries. The private exported inventory is not committed, but its hash and
counts are recorded with the persisted database metadata.

The original saved function inventory has 1,645 IDA-created default `sub_`
functions. `artifacts/symbol_translation_overlay.json` maps 886 of them to exact
script-table names and 271 to curated callback candidates, leaving 488
untranslated entries as an explicit work list. The overlay is generated by
`tools/generate_symbol_translation_overlay.py` and does not claim that any
names were written to IDA.

The unresolved list has now been profiled by region in
`artifacts/unresolved_function_profile.json`. Of the 488 entries, 335 sit in
gaps inside recognizable static library families: 150 in libjpeg, 144 in
FreeType, 14 in zlib, 11 in CyaSSL or its bundled crypto, 4 in GPC, 4 in
bzip2, 3 in GIF support, 2 in YAJL, 1 in the shared LibTomCrypt DES core, and
2 in minizip helpers. The fourth GPC entry is the small helper at `0xe01a0`,
outside the main contiguous GPC gap, which is called by `gpc_tristrip_clip`
and formats the library's `gpc malloc failure` diagnostic for tristrip node
creation. The extra bzip2 entry at `0xe02ac` is an unrolled comparison helper
called by the bundled decoder. The extra JPEG entry at `0xe0454` is called by
the marker parser and decodes image state. Another 19 are addresses referenced by
the ELF `.init_array` or `.fini_array`. Another 104 are compiler-generated
static cleanup wrappers. Their fixed global-object address and tail target
make the family unambiguous: 97 call `TString::clear`, 5 call
`TStringList::~TStringList`, and 2 call `TGraalVar::~TGraalVar`. These wrappers
do not have an independent source body to recover. One entry is a
four-byte compiler-generated branch veneer to the exact
`TCachedStream_get_minfilecachesize` callback at `0x1fa4fc`, and is not an
independent source function. One more entry is the 20-byte AArch64 PLT
resolver slot. The pre-persistence profile therefore leaves 28 application or
engine entries without a safe source-name recovery. Those 28 now have
high-confidence behavior-based role aliases in the base persisted copy. A
follow-up CyaSSL pass then mapped all eleven certificate and TLS entries to
seven high-confidence source-role matches and four descriptive aliases. IDA
also reclassified the branch veneer as a named thunk while rebuilding the
intermediate copy, leaving 448 default functions. The static-library pass
then left 421 default functions in v4. The final address-level accounting is
in `docs/IDA_RESIDUALS.md` and
`artifacts/ida_residual_profile.json`.

The profile records the address and size of every original unresolved entry,
but deliberately does not turn library-region membership into guessed source
names. This keeps the complete symbol translation separate from the harder
problem of naming compiler-created or static functions whose original names
were never stored in the APK.

`tools/generate_unresolved_function_profile.py` rebuilds this pre-persistence
report from the saved inventory, overlay, symbol export, and ELF section data.
It is a triage aid, not an IDA rename script. The base 459-entry report, the
intermediate 448-entry report, and the latest 421-entry persisted-copy report
are generated by
`tools/generate_ida_residual_profile.py` and described in
`docs/IDA_RESIDUALS.md`.

The CyaSSL pass is intentionally separate from the application-role artifact.
`artifacts/cyassl_static_role_audit_20260826.json` records the eleven
addresses, function sizes, callers, decompilation evidence, and source-role
comparison links. The seven high-confidence entries are `ConfirmSignature`,
the MD5, SHA-1, and SHA-256 transforms, `ProcessBuffer`, `PRF`, and
`ProcessPeerCerts`. The four medium-confidence names describe a verification
path loader, the record-MAC callback, record-MAC verification, and Finished
verify-data calculation. This keeps a useful IDA name available without
presenting a descriptive alias as an original ELF symbol.

The static-library role artifact records the 27 next-pass aliases with source
names, callers, sizes, and evidence. The zlib names use a `zlib_` prefix so
`_tr_init` and the other underscored source roles remain readable in IDA. The
bzip2 `fallbackSort`, `mainSort`, `mainGtU`, and `sendMTFValues` roles are tied
to their direct block-sorting and compression call sites. The two minizip
aliases match the internal central-directory and local-header helpers. The
three YAJL callbacks are identified by their placement in
`yajl_set_default_alloc_funcs` and their direct malloc, realloc, and free
bodies. Two old address-only category labels were corrected at the same time:
the allocator trio is YAJL, and `0x2b3be8` plus `0x2b3c64` are CyaSSL ASN
helpers. The historical profile remains unchanged, while the correction is
preserved in `artifacts/static_library_role_audit_20260826.json`.

A separate role-candidate artifact records 28 behavior-based aliases. The
original four cover the profiler helpers at `0xf9028`, `0xf9060`, and
`0xf9944`, plus the recursive worker at `0x213088` immediately before
`TGraalVar::loadFolder`. Thirteen more cover TBitmap GIF and JPEG stream
callbacks, the generated animation lexer fatal path, three TServerLevel
spatial-query predicates, a player draw-list predicate, a scroll-control
property resolver, and the actionnpc or activeplayer script-object resolver.
The final eleven cover the flex-style animation previous-state helper, a
generic draw-distance comparator, and the nine YAJL callbacks used by
`TGraalVar::readJSON`. All 28 candidates are now high-confidence role
assignments. IDA evidence ties the comparator at `0x20ac18` to both
`getnearestplayers` and `findnearestplayers`, which sort the runtime universe
list before returning their results. These aliases are analysis roles, not
recovered ELF names, and are not included in the applied semantic-label count.
The generator verifies that the role list covers all 28 entries in the
application or engine queue, with no missing or extra addresses.
The roles were applied and verified in the persisted disposable copy; the
candidate artifact remains review-only for the locked desktop database.
`tools/generate_unresolved_function_candidates.py` rebuilds the artifact and
`tools/ida_apply_unresolved_function_candidates.py` keeps the IDA step
review-only.

For a quick consistency check across the published summaries, run
`python3 tools/validate_research_archive.py`. It confirms that the artifacts
refer to the same ARM64 input hash, that the function and callback partitions
still add up, and that the role candidates cover the full 28-entry application
or engine queue. It is offline and does not require IDA.

When the IDA bridge is available, run
`tools/ida_apply_native_callback_candidates.py` in review mode first. It
resolves the existing names, checks each expected function address, reports
missing or mismatched functions, and only writes the proposed names and
evidence comments when `APPLY_RENAMES` is explicitly enabled.

For the complete pass, `tools/ida_apply_all_translations.py` runs the four
appliers in one session. Its two switches are both false by default. Set
`APPLY_BOUNDARIES = True` and `APPLY_RENAMES = True` only after reviewing the
reports. The boundary switch is required for the five native FDE ranges and
the twenty script-table ranges, including the two intentional function splits.

The inventory was generated from the active ARM64 database by
`tools/export_function_inventory.py`. It waits for auto-analysis, joins each
function start against the translated symbol export, and writes the result in
address order. Running it again against a different library revision should
produce a different input hash and must be kept as a separate export.

## Repeating the pass

Run the IDAPython script from IDA's Python console or with the IDA batch
runner. The input must be the same library revision used to build the IDB.
The script deliberately reports a byte or address mismatch instead of
silently applying names to a different build.

The generated summary should have `rename_failures` equal to an empty list.
If a future library revision produces a different count, keep its exports in
a separate directory and record the build identity in the accompanying
notes. Do not overwrite this table without preserving the old hash.
