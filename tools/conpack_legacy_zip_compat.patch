--- conpack_wsl.c.orig
+++ conpack_wsl.c
@@ -24,2 +24,2 @@
-static void zip_entry(Buf *z, Cent *c, const char *name, const unsigned char *data, size_t len) { Buf d = deflate_raw(data, len); c->name = strdup(name); c->crc = crc32(crc32(0, NULL, 0), data, (uInt)len); c->cs = (uint32_t)d.n; c->us = (uint32_t)len; c->off = (uint32_t)z->n; le32(z, 0x04034b50); le16(z, 20); le16(z, 0); le16(z, 8); le16(z, 0); le16(z, 0); le32(z, c->crc); le32(z, c->cs); le32(z, c->us); le16(z, strlen(name)); le16(z, 0); put(z, name, strlen(name)); put(z, d.p, d.n); free(d.p); }
+static void zip_entry(Buf *z, Cent *c, const char *name, const unsigned char *data, size_t len) { Buf d = deflate_raw(data, len); c->name = strdup(name); c->crc = crc32(crc32(0, NULL, 0), data, (uInt)len); c->cs = (uint32_t)d.n; c->us = (uint32_t)len; c->off = (uint32_t)z->n; le32(z, 0x04034b50); le16(z, 20); le16(z, 2); le16(z, 8); le16(z, 0xffff); le16(z, 0xffff); le32(z, c->crc); le32(z, c->cs); le32(z, c->us); le16(z, strlen(name)); le16(z, 0); put(z, name, strlen(name)); put(z, d.p, d.n); free(d.p); }
-static void zip_finish(Buf *z, Cent *c, int n) { uint32_t start = (uint32_t)z->n; for (int i = 0; i < n; ++i) { le32(z, 0x02014b50); le16(z, 20); le16(z, 20); le16(z, 0); le16(z, 8); le16(z, 0); le16(z, 0); le32(z, c[i].crc); le32(z, c[i].cs); le32(z, c[i].us); le16(z, strlen(c[i].name)); le16(z, 0); le16(z, 0); le16(z, 0); le16(z, 0); le32(z, 0); le32(z, c[i].off); put(z, c[i].name, strlen(c[i].name)); } uint32_t size = (uint32_t)z->n - start; le32(z, 0x06054b50); le16(z, 0); le16(z, 0); le16(z, n); le16(z, n); le32(z, size); le32(z, start); le16(z, 0); }
+static void zip_finish(Buf *z, Cent *c, int n) { uint32_t start = (uint32_t)z->n; for (int i = 0; i < n; ++i) { le32(z, 0x02014b50); le16(z, 0); le16(z, 20); le16(z, 2); le16(z, 8); le16(z, 0xffff); le16(z, 0xffff); le32(z, c[i].crc); le32(z, c[i].cs); le32(z, c[i].us); le16(z, strlen(c[i].name)); le16(z, 0); le16(z, 0); le16(z, 0); le16(z, 0); le32(z, 0); le32(z, c[i].off); put(z, c[i].name, strlen(c[i].name)); } uint32_t size = (uint32_t)z->n - start; le32(z, 0x06054b50); le16(z, 0); le16(z, 0); le16(z, n); le16(z, n); le32(z, size); le32(z, start); le16(z, 0); }
